<?php
require_once __DIR__.'/MarketDataCanonicalRawImportProofSpec.php';
$root=dirname(__DIR__,5);$evidence=null;$apply=in_array('--apply',$argv,true);foreach($argv as $a){if(strpos($a,'--evidence-id=')===0)$evidence=substr($a,14);}if(!$evidence||!preg_match('/^E-MD-B09-A002-\d{3}$/',$evidence))throw new RuntimeException('Use --evidence-id=E-MD-B09-A002-NNN [--apply]');
if(count(glob($root.'/docs/market_data/records/evidence/'.$evidence.'_*'))!==1)throw new RuntimeException('Governed evidence must exist before binding');
$p=MarketDataCanonicalRawImportTraceabilitySpec::matrixPath($root);$h=fopen($p,'r');$headers=fgetcsv($h);$bom=strpos($headers[0],"\xEF\xBB\xBF")===0;$headers[0]=preg_replace('/^\xEF\xBB\xBF/','',$headers[0]);$rows=[];while(($r=fgetcsv($h))!==false){if(count($r)===count($headers))$rows[]=array_combine($headers,$r);}fclose($h);$map=MarketDataCanonicalRawImportProofSpec::map($root);$seen=0;
foreach($rows as &$r){if(!isset($map[$r['rule_id']]))continue;$seen++;$r['coverage_status']='SATISFIED';$r['current_evidence_ids']=$evidence;$r['notes']=trim($r['notes'].' | MD-B09-A002: proof_binding='.$evidence.'; proof_family='.$map[$r['rule_id']].'; proof_chain=current authority -> actual canonical RAW implementation -> positive/fail-closed runtime proof -> residue -> governed evidence',' |');}unset($r);
if($seen!==139)throw new RuntimeException('binding count '.$seen);echo json_encode(['denominator'=>$seen,'evidence_id'=>$evidence,'apply'=>$apply],JSON_PRETTY_PRINT).PHP_EOL;if(!$apply)exit(0);$h=fopen($p,'w');$out=$headers;if($bom)$out[0]="\xEF\xBB\xBF".$out[0];fputcsv($h,$out);foreach($rows as $r)fputcsv($h,array_map(function ($k) use ($r) { return $r[$k]; }, $headers));fclose($h);

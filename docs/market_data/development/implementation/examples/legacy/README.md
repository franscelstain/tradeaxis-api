# Market Data Examples — Illustrative Only

Files in this folder are **non-authoritative illustrative/example artifacts**. They may preserve historical field names or example values to show artifact shape. Even when a filename contains `LOCKED`, it does not override current V2 owner contracts, Blueprint, Conformance Matrix, schema targets, or current audit verdict.

If an example conflicts with current strategy, the example is stale and must be regenerated; behavior must never be changed to make the example pass.

# Market Data Current State

> GENERATED — DO NOT EDIT MANUALLY

## Verification identity and coverage

- Verification epoch: `MD-REBASELINE-20260820-001`
- Required active traceability rows: **4038**
- Coverage denominator: **4009** (FINAL)
- SATISFIED: **3068**
- NOT_ASSESSED inside denominator: **941**
- CONDITIONAL_NOT_APPLICABLE / NOT_APPLICABLE: **29 / 29**
- CONDITIONAL_PENDING / APPLICABILITY_PENDING: **0 / 0**
- Transitional MANDATORY_OR_CONDITIONAL: **0**
- Verified coverage: **76.53% FINAL**
- Optional capability rules: **63**

## Current executable stage

- Stage: `MD-B18`
- Latest attempt / baseline: `MD-B18-A002` / `MD-B18-A002-BL001`
- State / verdict: `IN_PROGRESS` / `BLOCKED — MD-DEP-0015 (full suite waits on the corpus oracle via MD-DEP-0016); MD-DEP-0017 approved consolidated remediation execution (F-013/F-014/F-015/F-016/F-017/F-018); per-predicate review complete, consolidated remediation package drafted; Q1-Q6 approved by D005; Q6 proven E017 and Q5 ownership aligned E018; Q1-Q4 executable proof outstanding; A001 closure remains withdrawn`
- Residue/rework: `INCONCLUSIVE_RESIDUE_EVIDENCE`
- Dependency: `MD-DEP-0009` blocks B19; `MD-DEP-0014` RESOLVED by D003; **`MD-DEP-0015`** BLOCKING — owner-rebuilt instance verified; full suite fails only on the corpus oracle (F-011(b) → MD-DEP-0016); `MD-DEP-0016` old-data recovery OPEN_NON_BLOCKING; **`MD-DEP-0017`** BLOCKING — approved consolidated remediation execution (F-013/F-014/F-015/F-016/F-017/F-018); `MD-DEP-0012/0013` RESOLVED; `MD-DEP-0010/0011` remain downstream obligations
- Open finding: `F-MD-B18-A002-003` OPEN deferred B21; `004` PARTIALLY_RESOLVED remainder B22; `005` OPEN; `006` RESOLVED complete-parent normalization; `007` RESOLVED availability; `008` RESOLVED (D003 + executed-corpus aggregate); `009` RESOLVED (re-probed); `010` RESOLVED; **`011` PARTIALLY_RESOLVED** by D004: orphan subset correction probed; corpus oracle waits for the MD-DEP-0016 recovery; `012` RESOLVED — owner-rebuilt instance verified (E010); **`013` OPEN -- reopened E070** (E031-E050 had closed it on the publication path; its item 2 AS_KNOWN half -- nominal reason registry -- was never remediated; `MD-S050-R0014`/`MD-S019-R0071` returned to INCOMPLETE, eight of ten carried predicates still PROVEN; remediation via F-017 Gap B); **`014` RESOLVED** (E051; all four predicates rebound to guards that genuinely exercise the sense); **`015` RESOLVED** (E052-E057; all 14 of its own predicates PROVEN, consolidated remediation package G04/G07/G08/G09 complete); **`016` RESOLVED** (E058-E065; executable defect fixed G01, all 14 of its own predicates PROVEN including the F-013 carry-forward MD-S019-R0074); **`017` OPEN** — PAIRS 21–40: G01-A closed (E066, `MD-S082-R0218`/`MD-S082-R0224` PROVEN); G03 executable defect fixed (E067); closure audit (E068) found two residual gaps; Gap A (config identity) closed (E069); Gap B1 (AS_KNOWN read-model identity) decided by D-008 (Option 1, `market_data_read_product_v1`) and implemented (E071); Gap B2 (AS_KNOWN reason-registry identity, authority-determined fail-closed) implemented (E073, `REASON_REGISTRY_IDENTITY_UNAVAILABLE` marker, unconditional pre-comparison BLOCKED, storage refusal both modes); `MD-S050-R0016` PROVEN (E074, five-domain cumulative closure): `temporal_identity_hash`/`source_observation_manifest_hash`/`canonical_raw_input_hash` now fail-closed (unconditional for the first, trading-day-gated for the latter two, matching `ExpectedBarDecisionService`'s established `EXPECTED_BAR_NON_TRADING_DAY` precedent); `calendar_status_hash`'s calendar half proven already-throwing, its trading-status half and `event_factor_hash` confirmed authority-permits-legitimate-empty with no missing-input failure mode; `MD-S050-R0014`/`MD-S019-R0071` PROVEN claims corrected by E070 (AS_KNOWN over-claim, both IMPLEMENTATION_AND_PROOF_DEFECT, reviewed independently) and F-013 reopened, and remain INCOMPLETE after E073's/E074's independent re-review (fail-closed BLOCKED does not supply the positive binding/reproducibility either predicate's own wording requires; R0016's completeness does not by itself solve this); `MD-S050-R0005` remains INCOMPLETE (E073, IMPLEMENTATION_DEPENDENCY_UNAVAILABLE -- its negative clause's comparison became structurally unreachable once AS_KNOWN is unconditionally BLOCKED before any comparison); an attempted registration of a new reason code in the LOCKED `Reason_Codes_Registry.md` (`MD-S085`) was reverted after `STRATEGY_FREEZE`/`TRACEABILITY_MATRIX` gate failures showed it needs explicit user authorization this unit did not hold; G01-B closed (E075): `MD-S003-R0023`/`MD-S004-R0004` rebound from a hand-built exported row to the real writer -> `md_replay_daily_metrics` -> real export and PROVEN (both `PROOF_GUARD_GAP`, no production change; ten probes); 12 remain, owned by this finding: nine G05 (`MD-S002-R0005`/`R0006`/`R0007`/`R0008`, `MD-S003-R0025`, `MD-S004-R0002`/`R0003`/`R0005`/`R0008`), one G07 (`MD-S002-R0003`), one G09 (`MD-S065-R0003`), and `MD-S050-R0005`; **`018` OPEN** — PAIRS 41–68: 3 bases pass against a cutoff wall, 2 never exercise knowledge time, 3 are mistargeted, 1 leaves the null-reason class uncompared; 2 also carry F-013
- Change Impact Declaration: `CI-MD-B18-A002-001` — ISSUED
- Denominator: **114** (FINAL for every machine-checked criterion — no transitional applicability, no mixed-classification run, every reference row decided)
- SATISFIED / NOT_ASSESSED: **0 / 114**
- Mandatory / conditional-applicable: **114 / 0**
- Conditional-not-applicable / conditional-pending / transitional: **4 / 0 / 0**

## Stage state index

| Stage | Lifecycle | Verdict | Latest attempt | Baseline | Integrity gate |
|---|---|---|---|---|---|
| `MD-B00` | `DONE` | `PASS` | `MD-B00-A004` | `MD-B00-A004-BL001` | `PASS` (classification gate hardened with `UNEXPLAINED_REFERENCE` and `BINDING_COHERENCE`, both mutation-proven; self-test 12/46; full suite 1951/18238) |
| `MD-B01` | `DONE` | `PASS` | `MD-B01-A016` | `MD-B01-A016-BL001` | `PASS` (ownership-chain proof + promoted-predicate map + classification consistency + applicability + scope map, all mutation-proven) |
| `MD-B02` | `DONE` | `PASS` | `MD-B02-A001` | `MD-B02-A001-BL001` | `PASS` (provider-bootstrap traceability/proof gate + classification + relationship + documentation; mutation-proven) |
| `MD-B03` | `DONE` | `PASS` | `MD-B03-A003` | `MD-B03-A003-BL001` | `PASS` (drift detector, nullable-placeholder gate, test-path binding integrity, all mutation-proven) |
| `MD-B04` | `DONE` | `PASS` | `MD-B04-A002` | `MD-B04-A002-BL001` | `PASS` (config-foundation proof/traceability + classification + relationship + documentation; mutation-proven) |
| `MD-B05` | `DONE` | `PASS` | `MD-B05-A001` | `MD-B05-A001-BL001` | `PASS` (temporal-identity traceability/proof gates + classification + relationship + documentation; mutation-proven) |
| `MD-B06` | `DONE` | `PASS` | `MD-B06-A001` | `MD-B06-A001-BL001` | `PASS` (calendar/status traceability + exact proof + classification + relationship + documentation; mutation-proven) |
| `MD-B07` | `DONE` | `PASS` | `MD-B07-A002` | `MD-B07-A002-BL001` | `PASS` (exact B07 116/116 + all four governance gates + full suite 1946/18210; masking guard and normalization completeness assertion both mutation-proven) |
| `MD-B08` | `DONE` | `PASS` | `MD-B08-A002` | `MD-B08-A002-BL001` | `PASS` (exact B08 139/139 + failure-taxonomy surface 75/335 + all four governance gates + full suite; the new retention guard mutation-proven against the exact collapse that previously passed all 1946 tests) |
| `MD-B09` | `DONE` | `PASS` | `MD-B09-A003` | `MD-B09-A003-BL001` | `PASS` (B09 traceability + proof gates, all four governance gates, full suite 1951/18239; the source-JSON-path guard gap closed and mutation-proven) |
| `MD-B10` | `DONE` | `PASS` | `MD-B10-A001` | `MD-B10-A001-BL001` | `PASS` (exact 9-trigger deployed immutability + cumulative lifecycle/reconciliation/full-suite proof + rollback-safe deployed `REBUILT_AND_VERIFIED` repair + exact 1072 binding + post-binding controls) |
| `MD-B11` | `DONE` | `PASS` | `MD-B11-A003` | `MD-B11-A003-BL001` | `PASS` (B11 proof/traceability gates bound, all four governance gates, B11 surface 93/480, full suite 1953/18248 exit 0 against reachable MariaDB) |
| `MD-B12` | `DONE` | `PASS` | `MD-B12-A003` | `MD-B12-A003-BL001` | `PASS` (B12 proof/traceability/static gates bound, all four governance gates, B12 surface 78/247, full suite 1953/18247 exit 0 against reachable MariaDB) |
| `MD-B13` | `DONE` | `PASS` | `MD-B13-A001` | `MD-B13-A001-BL001` | `PASS` (in-session deployed-MariaDB targeted/full-suite proof + exact 33 binding + evidenced aggregate applicability + post-binding controls) |
| `MD-B14` | `DONE` | `PASS` | `MD-B14-A001` | `MD-B14-A001-BL001` | `PASS` — proof gate bound, self-test 11/11, 10 fail-closed probes and 8 closure-condition probes all caught |
| `MD-B15` | `DONE` | `PASS` | `MD-B15-A001` | `MD-B15-A001-BL001` | `PASS` — proof gate bound, self-test 11/11, 6 fail-closed probes and 8 closure-condition probes all caught |
| `MD-B16` | `DONE` | `PASS` | `MD-B16-A001` | `MD-B16-A001-BL001` | `PASS` — proof gate bound, self-test 11/11, 8 fail-closed and 8 closure-condition probes all caught |
| `MD-B17` | `DONE` | `PASS` | `MD-B17-A002` | `MD-B17-A002-BL001` | `PASS` — 246-entry proof map, atomic binding, self-test 11/11, 7 snapshot fail-closed guards, 8 closure-condition probes, affected B04 gates and post-binding full suite all pass |
| `MD-B18` | `IN_PROGRESS` | `BLOCKED — MD-DEP-0015 (full suite waits on the corpus oracle via MD-DEP-0016); MD-DEP-0017 approved consolidated remediation execution (F-013/F-014/F-015/F-016/F-017/F-018); per-predicate review complete, consolidated remediation package drafted; Q1-Q6 approved by D005; Q6 proven E017 and Q5 ownership aligned E018; Q1-Q4 executable proof outstanding; A001 closure remains withdrawn` | `MD-B18-A002` | `MD-B18-A002-BL001` | `NOT_READY_FOR_CLOSURE` — 91/114 bases present per `MarketDataReplayVerificationProofReadinessGate` (re-verified after E075 promoted `MD-S003-R0023`/`MD-S004-R0004` PROVEN by rebinding them to the real writer -> export path; superseded stale "89/114"; one transferred unproven to B22 by D005/E018, 23 remain without a reviewed basis -- 12 owned by F-017, 9 by F-018 and 2 by the reopened F-013); R0056 proven by the executed-corpus aggregate (6 probes caught); 4 stale N/A entries withdrawn from PROVEN; proof, readiness and binder FAIL on the 23 INCOMPLETE bases, and the self-test control is red for the same reason (plus one unrelated MISSING_NEGATIVE_PROOF:determinism_and_operations); normalization PASS on E008 (2 probes caught); all 69 pairs reviewed (PAIR 69, R0056, by the executed-corpus aggregate); closure FAIL on binding, A002 manifest and governed evidence (F005) |
| `MD-B19` | `IN_PROGRESS` | — | `MD-B19-A001` | `MD-B19-A001-BL001` | `PARTIAL` — proof map validated at 743/743 across 37 families, zero structural errors; **no family may now be called proven**: `F-MD-B19-A001-002` measured that a family-level guard assignment does not establish its members, and the three families previously recorded as proven carry one guard pair each for 52, 13 and 8 predicates. The gate now requires a reviewed per-predicate proof basis and reports **743/743 predicates without one**; 34/37 families also carry no guard at all |
| `MD-B20` | `NOT_STARTED` | — | — | — | `NOT_RUN` |
| `MD-B21` | `NOT_STARTED` | — | — | — | `NOT_RUN` |
| `MD-B22` | `NOT_STARTED` | — | — | — | `NOT_RUN` |

## Open dependencies and work records

- Open findings across every stage: `F-MD-B00-A001-001` — PARTIALLY_RESOLVED; `F-MD-B01-A001-001` — PARTIALLY_RESOLVED; `F-MD-B01-A014-001` — OPEN; `F-MD-B14-A001-001` — OPEN; `F-MD-B18-A002-003` — OPEN; `F-MD-B18-A002-004` — PARTIALLY_RESOLVED; `F-MD-B18-A002-005` — OPEN; `F-MD-B18-A002-011` — PARTIALLY_RESOLVED; `F-MD-B18-A002-013` — OPEN; `F-MD-B18-A002-017` — OPEN; `F-MD-B18-A002-018` — OPEN; `F-MD-B19-A001-002` — OPEN — total **12**
- Open dependencies: `MD-DEP-0003` — OPEN_NON_BLOCKING; owner `owning stages MD-B03/B15/B17/B19/B21/B22`; `MD-DEP-0004` — OPEN_NON_BLOCKING; owner `each stage at entry`; `MD-DEP-0009` — BLOCKING; owner `MD-B18`; `MD-DEP-0010` — OPEN_NON_BLOCKING; owner `MD-B21`; `MD-DEP-0011` — OPEN_NON_BLOCKING; owner `MD-B22 with MD-B18 supporting`; `MD-DEP-0015` — BLOCKING; owner `database owner (explicit user recovery decision)`; `MD-DEP-0016` — OPEN_NON_BLOCKING; owner `database owner (separate recovery track)`; `MD-DEP-0017` — BLOCKING; owner `MD-B18 approved bounded remediation under D-MD-B18-A002-005`
- Classification entry obligation (`MD-DEP-0004`), reference-only rows in mixed-classification runs by stage: `MD-B20` 9 — total **9**
- Registered current work records: **319** (BASELINE_LOCK=51, CHANGE_IMPACT_DECLARATION=46, DECISION=16, EVIDENCE=135, FINDING=42, STAGE_CLOSURE=5, STAGE_CLOSURE_MANIFEST=24)

## Exact resume

- Single exact next executable resume point: MD-B18-A002: **`F-MD-B18-A002-016` G01 (E-MD-B18-A002-058), G04 (E-MD-B18-A002-059), G05 (`MD-S050-R0017`, E-MD-B18-A002-060), G08 (`MD-S050-R0046`, E-MD-B18-A002-061) and G09 (`MD-S003-R0009`/`MD-S003-R0010`/`MD-S003-R0005`, E-MD-B18-A002-062, 2026-09-23T15:34:21+07:00) all closed.** G09: three predicates reviewed and proven independently, not bulk-promoted -- G09 closed because all three legitimately earned `PROVEN` on their own. `MD-S003-R0009` (inactive-now/active-then listing remains in the historical universe) and `MD-S003-R0010` (symbol change/reuse resolve through stable listing identity) confirmed `REBIND_ONLY` after independent review of the actual implementation and existing test corpus, not assumed: both rebound to `B18AntiSurvivorshipFixtureCorpusTest`'s dedicated fixtures, each independently mutation-probed against its own `TemporalIdentityRepository` production clause (the `delisted_date` OR-branch for R0009; the `pm.effective_to` upper bound for R0010), both caught, both byte-restored and sha256-verified, zero production change. `MD-S003-R0005` (provider outage cannot shrink the denominator) confirmed `PROOF_GUARD_GAP`, **not** rebind-only, per explicit verification rather than assumption: the prior candidate test only proved `FinalizeDecisionService` echoes back whatever its own fixture supplied, never that the real universe computation survives an outage. Audited `CoverageGateEvaluator::evaluateCaptured()` and confirmed the denominator is already correctly computed from the ticker-master universe, architecturally independent of provider delivery -- no production defect. Added the missing evaluator-level guard (`CoverageGateEvaluatorTest::test_evaluator_keeps_the_full_universe_as_the_denominator_when_the_provider_delivers_nothing`), mutation-probed and caught. Zero production code changed for any of the three. `PROVEN` 85 -> 88, `INCOMPLETE` 29 -> 26. **Issued E061 corrected by `E-MD-B18-A002-063` (2026-09-23T16:25:01+07:00), not edited:** E061 is `IMMUTABLE_AFTER_ISSUE` (`DOCUMENT_CHANGE_POLICY.md` §3; `DOCUMENT_RECORDING_STANDARD.md` §1), so a working-tree re-escape of its `pattern` field was reverted and E061 is byte-identical to commit `0caaf38`. E063 records its invalid-JSON `pattern` field (authoritative value: `B18ReplayAdmissibilityBoundaryTest::forbidden()`), its estimated `issued_at` (14:15:30 stated, 14:51:51 written), the replacement of its malformed rows in the three `MUTABLE_TRACEABLE` registries, and the `CURRENT_STATE.md` head that commit left overwritten by the generator's echoed path (the generator writes the file itself; do not redirect its stdout into it). **E061-U1 resolved (`F-MD-B18-A002-022` RESOLVED):** controlled revision `DOC-CHG-20260923-001` under owner decision `D-MD-B18-A002-007`, proven in `E-MD-B18-A002-064`. `DOCUMENT_INTEGRITY_GATE_STANDARD.md` gains an additive immutable-historical-integrity-exception section; `DOCUMENT_INTEGRITY_EXCEPTION_REGISTRY.json` holds one `ACTIVE` entry `MD-DOCEX-0001` binding E061 at its retained sha256 to E063 defect `E061-D1`; `MarketDataDocumentationIntegrityGate` validates the registry fail-closed (`INTEGRITY_EXCEPTION_REGISTRY`) and admits a `JSON_PARSE` failure only through a valid entry for that exact path, still reporting E061's raw failure. Every unexcepted malformed JSON remains a hard `FAIL`. The self-test's 15 new probes each fail exactly the checks they name (29 fail closed, 4 controls). E061 unchanged. **G01 (F-013 carry-forward) closed (`E-MD-B18-A002-065`, 2026-09-23T17:19:11+07:00):** `MD-S019-R0074` (`Determinism_Invariants_LOCKED.md:122`). Three clauses independently reconstructed, not trusted from F-013's `RESOLVED` status. Clause 1 (publication freezes exact identities) is `MD-S050-R0002`'s predicate content at the strategy layer -- re-probed (removed `temporal_identity_hash` from the comparison loop, that data set alone turned red, restored, sha256-verified). Clause 2 (as-known resolves only known revisions) required per-root proof, not one root standing in for seven: temporal/calendar/corporate-action/factor/config already probed in G05; formula/registry/serialization derive structurally from the already-probed config snapshot; `executable_build_identity` accepted live per the same R0002 precedent; source-observation manifest was genuinely uncovered -- new test (`SourceObservationAsKnownBoundaryTest::test_an_observation_acquired_after_the_cutoff_is_invisible_to_as_known_replay`) added and mutation-probed (removed `acquired_at<=knownAt`, turned red, restored, sha256-verified). Clause 3 (no leak) is the structural conjunction of 1+2. Zero production code changed. `PROVEN` 88 -> 89, `INCOMPLETE` 26 -> 25. **F-016 closure explicitly reviewed, not auto-declared:** all 14 of its own predicates confirmed `PROVEN`, the executable defect (backfill from current pointer) confirmed fixed in G01, no residue or blocker found. **`F-MD-B18-A002-016` is now formally `RESOLVED`.** `MD-DEP-0017` remains `BLOCKING` -- not resolved by F-016 closure, bundles `F-013`/`F-014`/`F-015`/`F-016` (all `RESOLVED`) through `F-018` (`F-017`/`F-018` remain `OPEN`). **State-alignment correction (read-only F-017 reconstruction, no predicate work):** row `MD-B18`'s Open finding cell still named `013`/`014`/`015`/`016` `OPEN` after all four were `RESOLVED`; corrected to name each finding's closing evidence. No predicate classification, ownership, applicability, or closure-criterion text was changed. **G01-A closed (`E-MD-B18-A002-066`, 2026-09-23T23:18:33+07:00):** `MD-S082-R0218`/`MD-S082-R0224`, each independently verified, not promoted merely because they share F-013 remediation. `MD-S082-R0218` ("current registry state must never leak into historical replay; alternate runs cannot impersonate the historical publication"): the recorded guard (`test_a_divergence_in_any_frozen_input_denies_pass`) proved comparison-completeness only, never whether the underlying actual read is live or frozen -- rebound to `ReplayVerificationServiceTest::test_registry_content_fields_come_from_decoded_capture_only_when_verified`, which directly asserts an honest empty string, never a live-config fallback, for read_model_version/serialization_version/executable_build_identity; the impersonation clause's existing guard (`B18AsKnownModeIsolationTest::test_an_as_known_result_claims_no_publication_and_records_its_own_mode`) was already correct and is unchanged. `MD-S082-R0224` ("current environment drift cannot change publication replay"): the recorded guard (`B18BeforeSealValidationTest` lineage-binding-mandatory-at-seal) proved a real but different property -- config_snapshot_id binding, not formula/read-model/serialization/build identity, and `verifyBoundContext()` does not require a `registry_versions` component to exist at all -- rebound to `test_formula_and_reason_registry_hash_come_from_the_registry_versions_component`. Each predicate independently mutation-probed on its own distinct code location (executable_build_identity's fallback branch for R0218; registryPayloadHash's initial value for R0224), each probe's sibling-isolation check confirming the other predicate's guard unaffected. Zero production code changed for either. `PROVEN` 89 -> 91, `INCOMPLETE` 25 -> 23. **G03 closed (`E-MD-B18-A002-067`, 2026-09-24T00:19:07+07:00):** `MD-S050-R0016` ("missing input is `BLOCKED`, not permission to query current/latest state"), classified `IMPLEMENTATION_DEFECT` + `PROOF_GUARD_GAP` from repository behaviour before any code changed. With a `VERIFIED` bound context, an empty or null run `observation_manifest_hash` replayed PASS/MATCH/ADMISSIBLE: `actualBoundInputContext()` reads the observation identity and `canonical_raw_input_hash` off the run row, and Seal admits an `analytical_remediation_current` publication with no acquisition manifest (`ANALYTICAL_ONLY`), so the state is reachable; `ReplayResultRepository::assertModeInputs()` required the seven identities of `AS_KNOWN` results only. Fixed minimally, nothing fabricated: `replayAdmissibility()` blocks any `BOUND_INPUT_FIELDS` value that resolved empty as `REPLAY_BOUND_INPUT_INCOMPLETE` naming the field; the write check covers both modes. Rebound to `B18ReplayComparisonExhaustivenessTest::test_a_verified_publication_missing_a_required_input_is_blocked` (8 per-input cases) and `::test_an_unavailable_observation_identity_is_not_filled_from_current_state`, plus seven `PUBLICATION_EXACT` direct-write cases. Four probes (block removed, one field dropped, current-publication fallback, write guard AS_KNOWN-only) each red on exactly their targets, byte-restored and sha256-verified. Six `ReplayVerificationServiceTest` tests had pinned a `VERIFIED` stub with no components, a state `bind()` cannot produce; the stub was completed, the gate not weakened. Full `tests/Unit/MarketData` 2456 tests, 7 failures, all the `MD-DEP-0015` corpus-oracle baseline. `PROVEN` 91 -> 92, `INCOMPLETE` 23 -> 22; formal `0/114` `SATISFIED` unchanged. **G03 closure audit (`E-MD-B18-A002-068`, 2026-09-24T07:30:22+07:00, read-only, before commit): the E-067 promotion was premature; `MD-S050-R0016` returned to `INCOMPLETE` (`PROVEN` 92 -> 91, `INCOMPLETE` 22 -> 23).** E-067 is not edited; its guards and probes stand for the inputs they name. Calendar/status and event/factor sub-components are outside the admission check: binding enforces them with named missing paths and a context missing one is never `VERIFIED`. Residual gap A: `Platform_Config_Registry_LOCKED.md` makes a run without a non-null config snapshot ID *and hash* `CONFIG_UNBOUND` with replay `BLOCKED`, but a run with an ID and no `config_hash` replays ADMISSIBLE/PASS on the `CONFIG_IDENTITY_UNRECORDED` marker and the direct-write check accepts the marker; a temporary admission block turned 41 exhaustiveness and 11 `ReplayVerificationServiceTest` tests red and was reverted (service sha256 `cb2d14dc...2d6`) because MariaDB stopped between runs (not restarted). Residual gap B (owner decision): AS_KNOWN reads `governance.read_model_version`, which config does not define, so every AS_KNOWN replay records it empty and can PASS; the direct-write check omits it in both modes; AS_KNOWN reason-registry identity hashes nominal names. Export-admission asymmetry recorded as out of R0016 scope. **Gap A closed (`E-MD-B18-A002-069`, 2026-09-24T08:31:09+07:00):** `configIdentityForRun()`'s `CONFIG_IDENTITY_UNRECORDED` placeholder for a run with a config snapshot ID but no hash or snapshot reference is now blocked at both boundaries -- `replayAdmissibility()` returns `BLOCKED`/`REPLAY_CONFIG_UNBOUND` (scoped inside the "publication to reproduce" branch, so a non-readable `EXPECTED_DEGRADE` run is not converted into a config-unbound case), and `ReplayResultRepository::assertModeInputs()` refuses the marker in both modes. No value fabricated; no current/latest fallback added -- confirmed by a dedicated no-fallback test and a probe that introduced a hypothetical fallback and watched it turn red. Two real-path tests plus one direct-write case added; three probes (admission check removed, storage check bypassed, fallback introduced) each isolated to their own target, byte-restored, sha256-verified. Full `tests/Unit/MarketData`: 2459 tests, 34569 assertions, 7 failures -- exact match to the `MD-DEP-0015` corpus-oracle baseline, zero new failures. `MD-S050-R0016` remains `INCOMPLETE`: `PROVEN`/`INCOMPLETE` unchanged at 91/23 by explicit instruction, since Gap B (`AS_KNOWN` read-model version and reason-registry identity) is untouched and needs an owner decision (fail-closed `AS_KNOWN` vs. binding a real as-known identity), not an implementation choice. **Gap B1 decided (`D-MD-B18-A002-008`, 2026-09-24T11:51:50+07:00, owner Option 1):** AS_KNOWN `read_model_version` binds the replay/read-product contract version of the artifact being rendered, current canonical identity `market_data_read_product_v1` -- not the runtime code version, build identity, config snapshot at the cutoff, the `MD-S082` minimum consumer version, or a new config value; a historical artifact rendered under V1 stays bound to V1. Reverified against `MD-S021`, `MD-S045`, `MD-S050` (:17/:30/:33/:107), `MD-S004` and `MD-S082` (:289/:291) with no conflict; no strategy byte, config key or change-log entry. Implementation not started. Gap B2 (reason-registry identity) remains authority-determined fail-closed and unimplemented. Read-only locator: `MD-S050-R0014` is `PROVEN` on publication-mode-only evidence while its predicate covers every fixture/manifest, and AS_KNOWN reason-registry is nominal and read-model unbound -- over-claimed now, independent of any Gap B implementation; `MD-S019-R0071` rests on the same basis under a both-mode invariant; `MD-S050-R0005`'s claim is valid, but its negative guard becomes unreachable once B2 blocks AS_KNOWN, so it is rebound inside the B2 unit. Proof basis unchanged at 91/23; `MD-S050-R0016` `INCOMPLETE`; formal `0/114` unchanged. **R0014/R0071 claim correction (`E-MD-B18-A002-070`, 2026-09-24T13:23:51+07:00):** both predicates cover AS_KNOWN (R0014 every fixture/manifest; R0071 Invariant 14 names both modes), and `E-MD-B18-A002-050` proved them on the `PUBLICATION_EXACT` path only, although F-013 item 2 records the reason-registry identity as nominal in both modes. Executed: changing one of 437 `eod_reason_codes` rows and adding a code left AS_KNOWN `reason_registry_hash` and the as-known `snapshot_hash` unchanged; `read_model_version` is empty. A constant AS_KNOWN reason hash (the package's R0014 probe) was caught by none of the bound guards or AS_KNOWN suites; the same constant in `PUBLICATION_EXACT` turned the bound negative guard red; both files byte-restored and sha256-verified. `MD-S050-R0014` and `MD-S019-R0071` (independently, reason-registry ingredient only) classified `IMPLEMENTATION_AND_PROOF_DEFECT` and returned to `INCOMPLETE`: `PROVEN` 91 -> 89, `INCOMPLETE` 23 -> 25. `F-013` reopened: its closure rested on all ten carried predicates PROVEN and the defect is its own item 2; E-050 stays as issued, its publication-mode facts valid. No production or test change; D-008 and formal `0/114` unchanged. **Gap B1 implemented (`E-MD-B18-A002-071`, 2026-09-24, `D-MD-B18-A002-008`):** added `MarketDataReadProductService::READ_MODEL_VERSION` (= `market_data_read_product_v1`), the natural MD-S021 owner, replacing two of that class's own pre-existing literal duplicates; the four other already-proven publication-path call sites untouched. `AsKnownReplaySnapshotService::capture()` now binds that constant directly instead of the nonexistent `governance.read_model_version` key; `ReplayResultRepository::assertModeInputs()` requires `read_model_version` in both modes (Gap B2's `reason_registry_hash` untouched). Proof: a positive test pinned independently to the literal string (catching a wrong value in the constant itself, which comparing only to the constant would not); a config-independence test against a direct `capture()` call (the full verifier path independently rejects unrelated `market_data.*` drift via `EodBarsIngestService::assertConsumedConfiguration()`, confirmed by hitting that guard first); direct-write refusal in both modes. Three probes (revert to config-derived, wrong canonical literal, storage check removed), each isolated to its target, byte-restored, sha256-verified; publication-mode control suites unaffected by every probe. Full `tests/Unit/MarketData`: exact match to the `MD-DEP-0015` 7-failure baseline, zero new failures; MariaDB verified reachable, not started/restarted. `MD-S050-R0016`, `MD-S050-R0014` and `MD-S019-R0071` remain `INCOMPLETE` -- Gap B2 (AS_KNOWN reason-registry, fail-closed) is required for all three and is not implemented; not promoted. `PROVEN`/`INCOMPLETE` unchanged at 89/25; `D-008` and formal `0/114` unchanged. **Gap B2 implemented (`E-MD-B18-A002-073`, 2026-09-24T21:09:22+07:00):** `Reason_Codes_Registry.md` (`MD-S085`) re-read in full; its five "Registry rules" cover naming, one-code-one-meaning, description drift and deprecation only, no revision/version/effective-time/recorded-time concept anywhere -- no historical reason-registry state an as-known replay could legitimately bind. Added `AsKnownReplaySnapshotService::REASON_REGISTRY_IDENTITY_UNAVAILABLE`, replacing the old `reason_registry_hash` (a hash of two hardcoded constant arrays plus a config key that never existed -- the same shape E-069 found for the config identity). `ReplayVerificationService::verifyAsKnownAgainstFixture()` gained an early-return block, placed before the execution service is invoked (unlike `PUBLICATION_EXACT`'s admissibility check, which runs after the actual state is built -- AS_KNOWN's canonicalizer has real side-effect/expense concerns the block must not trigger): every as-known replay whose captured snapshot carries the marker is persisted directly as `BLOCKED`/`NOT_ADMISSIBLE`, unconditionally, before any fixture comparison executes. `ReplayResultRepository::assertModeInputs()` gained a direct-write refusal for the marker, in both modes. Real-path proof: base (non-perturbed) scenario `BLOCKED` not `PASS`; fixture-perturbed scenario still `BLOCKED` not `MISMATCH`/`FAIL`; inserting a new row into the live `eod_reason_codes` table does not make a historical replay admissible; direct-write refusal confirmed both modes. Four probes (restore old fabricated hash, bypass the admission check, live-table current/latest fallback, storage check removed), each isolated to its target, byte-restored and sha256-verified; `B18AsKnownSnapshotIsolationTest`/`ReplayVerificationServiceTest` (publication control) unaffected by every probe. A real defect surfaced by the first full regression (2467 tests, 8 failures) and fixed: the BLOCKED metric's `final_reason_code` was first the bare literal `REPLAY_REASON_REGISTRY_IDENTITY_UNAVAILABLE`; `EmittedReasonCodeRegistrationTest` (a derived, non-hand-written scan) proved it was emitted but registered nowhere. Adding it to `Reason_Codes_Seed.sql` and the paired `Reason_Codes_Registry.md` caused two further failures -- `GovernanceGateReadOnlyExecutionTest`'s `STRATEGY_FREEZE` check (`Reason_Codes_Registry.md` is `MD-S085` under the current freeze, `MARKET_DATA_STRATEGY_FREEZE_MANIFEST.json`, sha1 pinned) and its `TRACEABILITY_MATRIX` check (the insertion shifted twenty positionally-derived `MD-S085-R####` ids) -- because the one prior precedent for touching this exact document (`DOC-CHG-20260823-001`/`D-MD-20260823-01`) required an explicit, quoted user authorization instruction first, which this unit's governing instruction (authorizing Gap B2's production remediation, explicitly not `G01-B`) does not supply. Both edits fully reverted (`git checkout` refused by the harness's destructive-action classifier; inserted lines removed individually instead); `Reason_Codes_Registry.md` sha1 reconfirmed identical to the frozen manifest (`3BB61139B60926D99A7E0940636A830C99627030`); the one row seeded into `tradeaxis_testing.eod_reason_codes` during the attempt deleted. `final_reason_code` set to `null` instead, matching `replayAdmissibility()`'s own `NOT_ADMISSIBLE`-override precedent (none of `CONFIG_IDENTITY_UNRECORDED`/`REPLAY_BOUND_INPUT_INCOMPLETE`/`REPLAY_CONFIG_UNBOUND` assign a bespoke `final_reason_code` either); cause remains legible via `mismatch_summary` and `reason_registry_hash`. No new reason code registered; no LOCKED strategy document modified. `MD-S050-R0005`: every as-known replay is now BLOCKED before any comparison, unconditionally (confirmed by inspection -- `capture()`'s assignment has no conditional path around the marker), so R0005's negative clause (a genuine as-known divergence must be caught as `MISMATCH`/`FAIL`) has no reachable comparison to exercise -- not force-replaced with a same-named guard proving a different claim; moved `PROVEN` -> `INCOMPLETE` (positive kept, negative `null`, `IMPLEMENTATION_DEPENDENCY_UNAVAILABLE`). `MD-S050-R0014`/`MD-S019-R0071` reviewed independently, both remain `INCOMPLETE` -- fail-closed `BLOCKED` does not supply the positive binding/reproducibility either predicate's own wording requires; R0071's narrower current-state-must-never-leak guarantee is proven for this ingredient, which is not its own reproducibility claim. `MD-S050-R0016` not promoted -- Gap A+B1+B2 all implemented and proven for their specific inputs, AS_KNOWN's config path confirmed fail-closed by construction (bounded read-only check, not an executed probe), but calendar/status, event/factor, temporal universe, source-observation and canonical-raw-input captures were not independently probed for a missing-input case in this unit. Full `tests/Unit/MarketData`: 2467 tests, 34637 assertions, 7 failures, 0 errors -- exact match to the `MD-DEP-0015` corpus-oracle baseline, zero new failures; two intermediate runs (8 and 9 failures) were real defects of this unit's own making, diagnosed and fixed, not MariaDB contention; MariaDB verified reachable before each attempt, not started/restarted. `PROVEN` 89 -> 88, `INCOMPLETE` 25 -> 26 (`MD-S050-R0005` only transition); `D-008`, `E-050`, `E-067` through `E-072` and `MARKET_DATA_STRATEGY_FREEZE_MANIFEST.json` unchanged; formal `0/114` unchanged. **Five-domain cumulative closure (`E-MD-B18-A002-074`, 2026-09-25T00:06:18+07:00):** the B2 BLOCKED representation (`final_reason_code = null`) was first audited read-only and classified `CURRENT_NULL_REASON_DESIGN_IS_AUTHORITY_CONFORMING` -- `Run_Artifacts_Format_LOCKED.md`'s `replay_result.json` minimum-fields spec does not list `final_reason_code` at all, only `comparison_note`/`mismatch_summary`; its own locked rule authorizes `BLOCKED` "when replay cannot run because fixture/context/runtime prerequisites are missing"; the cause is traced live through the persisted DB row, `VerifyReplayCommand`'s output block and `MarketDataEvidenceExportService`'s export, none of which hide it. Then the five domains were reconstructed: `temporal_identity_hash` (`readProjectedUniverseAsOf()` never throws on a whole-system-empty universe -- ground-truth captured against a minimal calendar+config-only world) and `source_observation_manifest_hash`/`canonical_raw_input_hash` (neither AS_KNOWN manifest method throws on zero rows on a trading day, distinct from a recorded provider outage) confirmed `IMPLEMENTATION_DEFECT` and fixed: `AsKnownReplaySnapshotService::TEMPORAL_IDENTITY_UNAVAILABLE`/`SOURCE_OBSERVATION_UNAVAILABLE` markers, the latter gated on `calendar_context.is_trading_day` matching the existing `ExpectedBarDecisionService::decide()` precedent (`EXPECTED_BAR_NON_TRADING_DAY`) so a non-trading day's correct zero stays a real hash. `calendar_status_hash`'s calendar half confirmed `ALREADY_PROVEN_BY_EXECUTING_UPSTREAM_GUARD` (`MarketCalendarRepository::sessionContext()` already throws `MARKET_CALENDAR_EVIDENCE_MISSING`, now proven executing by a new test); its trading-status half and `event_factor_hash` confirmed authority-permits-legitimate-empty, with no missing-input failure mode to test (no counterpart of `is_trading_day` exists for corporate-action/factor-set data). `verifyAsKnownAgainstFixture()`'s admission check generalized from a single reason-registry field check into a small lookup table covering all three markers, one early-return block, reason text selected by whichever field triggered; `ReplayResultRepository::assertModeInputs()` gained matching storage-boundary refusals. Four probes (temporal empty-check bypassed, source-observation empty-check bypassed, admission-check lookup narrowed, storage checks removed), each isolated to their own target (the admission-check probe correctly turned both new positive tests red, naming the wrong cause, since the reason-registry marker still fires unconditionally underneath), byte-restored and sha256-verified. Cumulative review of every canonical required input in `BOUND_INPUT_FIELDS` confirms each is now present-when-required-and-checked, structurally protected, or authority-confirmed legitimately empty -- no current/latest fallback anywhere. Full `tests/Unit/MarketData`: 2475 tests, 34674 assertions, 7 failures, 0 errors -- exact match to the `MD-DEP-0015` baseline, zero new failures. `MD-S050-R0016` promotes to `PROVEN`; `MD-S050-R0005`/`R0014`/`R0071` explicitly untouched, unaffected by R0016's completeness. `PROVEN` 88 -> 89, `INCOMPLETE` 26 -> 25; `MarketDataReplayVerificationProofReadinessGate` re-run directly confirms 89/114 bases present. `D-008`, `E-050`, `E-067` through `E-073` and `MARKET_DATA_STRATEGY_FREEZE_MANIFEST.json` unchanged; formal `0/114` unchanged. **G01-B implemented (`E-MD-B18-A002-075`, 2026-09-25T07:42:08+07:00):** `MD-S003-R0023` ("Per-run evidence") and `MD-S004-R0004` ("Every row/export binds listing identity, ..., factor/formula versions, and lineage") reconstructed independently from the repository; both `PROOF_GUARD_GAP`, no implementation defect, no production change (the earlier `REBIND_ONLY` reading of `R0023` corrected -- no existing guard drove the real writer). The recorded guards exported a hand-built metric through a mocked repository and stayed 14/14 green with the real writer no longer persisting `event_factor_hash`. New `B18ReplayPersistedEvidenceBindingTest` drives the real `ReplayVerificationService` -> real `ReplayResultRepository::upsertMetric()` -> `md_replay_daily_metrics` -> real `EodEvidenceRepository` -> real `MarketDataEvidenceExportService`, stubbing only the writer's inputs: all twelve frozen identities persisted and exported exactly, eleven per-source perturbations each moving exactly the identities they feed, and identical identities after live config/build drift. `R0004`: authority reads listing identity at row and export granularity, so both are proven -- universe identity at export level and a positive `listing_id` on every exported as-known universe, source and factor row, each source row also carrying an availability timestamp distinct from the trade date; factor identity (`event_factor_hash`, per-row `factor_set_id`, lineage `factor_set_hash`) proven independent of formula identity in both modes; lineage asserted with content (authority enumerates no lineage fields -- the verifier's own lineage block is held, none invented). Ten probes, each byte-restored and sha256-verified, each turning exactly its own guards red (a stripped per-row `listing_id` failed after the export-level universe-hash assertions had passed; a constant universe identity left the per-row test green). Targeted: 17/156, 16/89, 14/143, 53/257, 3/27 plus six governance self-tests; full `MarketData` suite not run (no production change). `PROVEN` 89 -> 91, `INCOMPLETE` 25 -> 23; readiness gate run directly: 91/114. `R0005`/`R0014`/`R0071`, `D-008`, `E-050`, `E-067` through `E-074` and `MARKET_DATA_STRATEGY_FREEZE_MANIFEST.json` unchanged; formal `0/114` unchanged. **Ownership of the 23 remaining:** `F-017` 12 (nine G05, one G07, one G09, `MD-S050-R0005`), `F-018` 9, reopened `F-013` 2. **G09 stopped (`E-MD-B18-A002-077`, 2026-09-25T08:55:45+07:00):** `MD-S065-R0003` ("reruns must use the registry version effective for the requested trade date or explicitly documented override") reconstructed from the repository; classification `AUTHORITY_AMBIGUITY`, no promotion, no probe, no code or test change. The earlier reconstruction ("all three `resolveForRun` callers pass the requested date, so production conforms") is disproven: there are four call sites, and the two that pass no cutoff (`getOrCreateOwningRun`, `createPromoteRunFromSeed`) hash the live configuration and reuse the governing snapshot only when the hashes are equal, otherwise minting a new snapshot with the live content stamped effective from the requested date. Executed on the real path: a seed run for 2026-03-24 bound version A, version B became live and was recorded effective 2026-04-10, and `createPromoteRunFromSeed` for 2026-03-24 bound B (a third snapshot stamped effective 2026-03-24). The platform also rejects the literal alternative (`assertConsumedConfiguration()`: a run must execute its bound configuration, which must equal live config), authority does not define the "explicitly documented override", and the recompute and correction contracts treat a rerun under changed configuration as legitimate. Implementation cannot choose; three options recorded in E077 (recognise current behaviour as the documented override via controlled strategy revision; default to the historical version and block; effective-dated registered changes). The "today's resolved config" source comment was checked and is accurate, not stale. The `MD-S065-R0003` proof-basis narrative was corrected (still `INCOMPLETE`). `R0005`/`R0014`/`R0071`, `F-013`, `F-018`, the G05/G07 predicates, `D-008`, `E-050`, `E-067` through `E-076` and the freeze manifest unchanged; `91`/`23`; formal `0/114` unchanged. **Next canonical work:** the owner decision on `MD-S065-R0003`; `G09` cannot proceed as proof-only work and no other `F-017` unit is started while it is open. Not started. Registering `REPLAY_REASON_REGISTRY_IDENTITY_UNAVAILABLE` in the LOCKED `Reason_Codes_Registry.md` remains available as a separate, explicitly user-authorized unit if a bespoke `final_reason_code` for the AS_KNOWN-reason-registry-BLOCKED state is later wanted; no currently `INCOMPLETE` predicate requires it. `MD-DEP-0017` remains `BLOCKING` and bundles `F-013` (`OPEN`, reopened E070) and `F-017`/`F-018` (`OPEN`) -- `F-017`'s own executable defect (Gap A/B1/B2/five-domain closure/G01-B) is now fully remediated, but it stays `OPEN` on its 12 remaining predicates (nine G05, one G07, one G09, `MD-S050-R0005`); `R0014`/`R0071` belong to the reopened `F-013`. Not started here. No data_260914 action, new attempt, strategy change or relock. No commit or push performed by this session. Return `MD-B19-A001` only after valid B18 closure and `MD-DEP-0009` resolution. **Runtime prerequisite chronology below:**
- Current stage source: `MD_IMPLEMENTATION_STAGE_REGISTER.md`
- Pre-epoch W00..W22 verdicts: **historical-only**

# Market Data Implementation Stage Register

> **Status:** CURRENT ORCHESTRATION INDEX
> **Verification Epoch:** `MD-REBASELINE-20260820-001`

Pre-epoch `W00..W22` PASS/FAIL/PARTIAL/DONE/PROVEN/CONFORMANT is historical-only. `NOT_STARTED` here does not mean code does not exist; it means current revalidation has not been opened.

| Stage | Maps to frozen scope | Lifecycle state | Verdict | Latest attempt / Work ID | Baseline ID | Strategy coverage | Residue state | Integrity gate | Dependency | Open finding | Closure manifest | Resume from |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `MD-B00` | `W00` — Preflight dan implementation ledger | `DONE` | `PASS` | `MD-B00-A002` | `MD-B00-A002-BL001` | `0/0` | `CONFORMANT_NO_HARMFUL_RESIDUE_FOUND` | `PASS` (validity + completeness, mutation-proven) | `MD-DEP-0003` | `F-MD-B00-A001-003` (P3, deferrable) | `SC-MD-B00-A002-001` | return to `MD-B01` for `MD-DEP-0004` remediation |
| `MD-B01` | `W01` — Kunci scope, boundary, dataset start, development frontier, activation semantics, dan non-goals | `IN_PROGRESS` | — | `MD-B01-A004` | `MD-B01-A004-BL001` | `21/170` (post re-derivation denominator) | `CONFORMANT_NO_HARMFUL_RESIDUE_FOUND` | `PASS` | `MD-DEP-0004` (narrowed to stage-ownership half) | `F-MD-B01-A001-001` (PARTIALLY_RESOLVED); `F-MD-B01-A003-001` (P2) | — | open `MD-B01-A005` — re-derive `primary_stage` by proof ownership |
| `MD-B02` | `W02` — Kunci Yahoo bootstrap dan provider-neutral ports | `NOT_STARTED` | — | — | — | `0/38 mandatory + 0/4 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B03` | `W03` — Bangun migration framework, additive schema skeleton, repository interfaces, reason registry, dan test harness skeleton | `IN_PROGRESS` | — | `MD-B03-A001` | `MD-B03-A001-BL001` | `0/0` | `CONFORMANT_NO_HARMFUL_RESIDUE_FOUND` | `PASS` (post-restoration; drift detector corrected and mutation-proven) | `MD-DEP-0003` | `F-MD-B00-A001-001` (Class S half) | — | **PAUSED** — dependency and post-restoration database proof retained (`E-MD-B03-A001-001`, `E-MD-B03-A001-002`); CI record and closure prerequisites still required before `DONE` |
| `MD-B04` | `W04` — Bangun immutable configuration snapshot dan semantic version bindings | `NOT_STARTED` | — | — | — | `0/184 mandatory + 0/1 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B05` | `W05` — Bangun temporal issuer/instrument/listing/symbol/provider mapping **serta temporal sector membership foundation** | `NOT_STARTED` | — | — | — | `0/76 mandatory + 0/1 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B06` | `W06` — Bangun calendar/session/status expectation | `NOT_STARTED` | — | — | — | `0/64 mandatory + 0/1 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B07` | `W07` — Bangun immutable source observations dan acquisition ports/adapters | `NOT_STARTED` | — | — | — | `0/135` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B08` | `W08` — Bangun resilience, retry/backoff/rate limit, manual recovery, quarantine, dan failure taxonomy | `NOT_STARTED` | — | — | — | `0/132` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B09` | `W09` — Bangun import-only, canonical `RAW`, invalid-row, dedup/conflict, dan candidate persistence | `NOT_STARTED` | — | — | — | `0/71 mandatory + 0/1 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B10` | `W10` — Bangun immutable publication state machine, manifest, seal, pointer, correction, supersession, dan no-in-place-rewrite | `NOT_STARTED` | — | — | — | `0/452 mandatory + 0/1 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B11` | `W11` — Bangun verified corporate-action event/factor lifecycle dan anomaly-only detector | `NOT_STARTED` | — | — | — | `0/108 mandatory + 0/1 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B12` | `W12` — Bangun coherent `RAW`/`STRUCTURAL_ADJUSTED`/`TOTAL_RETURN` product engine | `NOT_STARTED` | — | — | — | `0/33` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B13` | `W13` — Bangun actual/proxy daily market metrics | `NOT_STARTED` | — | — | — | `0/19` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B14` | `W14` — Bangun deterministic indicator engine dan correction dependency graph | `NOT_STARTED` | — | — | — | `0/73 mandatory + 0/6 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B15` | `W15` — Bangun temporal coverage expectation/delivery gate | `NOT_STARTED` | — | — | — | `0/88 mandatory + 0/2 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B16` | `W16` — Bangun explainable row-level data usability | `NOT_STARTED` | — | — | — | `0/28 mandatory + 0/2 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B17` | `W17` — Bangun atomic versioned market-data read product dan freshness/readiness gateway | `NOT_STARTED` | — | — | — | `0/93 mandatory + 0/1 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B18` | `W18` — Bangun exact-publication dan as-known replay | `NOT_STARTED` | — | — | — | `0/27 mandatory + 0/2 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B19` | `W19` — Bangun daily/backfill/correction/replay operations, locking, observability, evidence export, dan recovery | `NOT_STARTED` | — | — | — | `0/219 mandatory + 0/1 optional` | `NOT_ASSESSED` | `NOT_RUN` | `MD-DEP-0003` | `F-MD-B00-A001-001` (Class S half) | — | begin/revalidate stage; 7 test files still bound to split-sealed composites |
| `MD-B20` | `W20` — Implementasikan supplemental session snapshot hanya bila feature state dinyatakan aktif | `NOT_STARTED` | — | — | — | `0/0 mandatory + 0/30 optional` | `NOT_ASSESSED` | `NOT_RUN` | — | — | — | begin/revalidate stage |
| `MD-B21` | `W21` — Global schema/config/code/test/ops convergence, backfill, constraint hardening, dan full semantic proof | `NOT_STARTED` | — | — | — | `0/0` | `NOT_ASSESSED` | `NOT_RUN` | `MD-DEP-0003` | `F-MD-B00-A001-001` (Class S half) | — | `MD-DEP-0001`/`0002` now RESOLVED; begin/revalidate stage |
| `MD-B22` | `W22` — Independent implementation audit, pre-activation catch-up, operational validation, dan relock | `NOT_STARTED` | — | — | — | `0/0` | `NOT_ASSESSED` | `NOT_RUN` | `MD-DEP-0003` | `F-MD-B00-A001-001` (Class S half) | — | begin/revalidate stage; audit instruments read removed composites |

## Closure rule

A stage may become `DONE` only with current Attempt/Baseline/Epoch evidence, required strategy coverage, tests, residue verdict, dependencies resolved or explicitly governed, required correlated Change Impact Declaration for material attempts, complete/valid registered cross-record relationships, integrity/relationship gates that enforce the current invariants, and closure manifest. Old Wxx verdicts cannot populate these fields.

## Current orchestration after `DOC-CHG-20260821-001`

The 2026-08-21 governance revision changed relationship-completeness, Change Impact Declaration, exact-resume, and closure/gate invariants. `MD-B00` governance re-entry is complete: `MD-B00-A002` closed `PASS` under the revised invariants with `SC-MD-B00-A002-001`, which supersedes `SC-MD-B00-A001-001` for sufficiency. The prior baseline, evidence, and closure manifest remain immutable and unedited.

What `MD-B00-A002` actually fixed: the relationship integrity gate enforced validity only and would return `PASS` on an empty registry, which the revised standard names as a forbidden result. It now enforces completeness as well, derived from the canonical relationship-bearing record columns and from baseline predecessor declarations. Running it exposed six current-epoch records absent from `WORK_RECORD_REGISTRY.csv` and 17 missing relationship rows; both were reconciled by rerunning the check until it reported zero rather than by inspection. Three completeness mutations were added to the gate self-test, all failing closed.

- Logical blocked stage: `MD-B01`, blocked by the stage-ownership half of `MD-DEP-0004`. The predicate half was remediated at `MD-B01-A004`.
- Active remediation stage: none. `MD-B03-A001` remains open with its dependency-remediation evidence retained, but it is not the current executable work.
- **Single exact next executable resume point:** open `MD-B01-A005` and complete the remaining half of `MD-DEP-0004` — re-derive `primary_stage` by proof ownership under `STRATEGY_IMPLEMENTATION_TRACEABILITY_STANDARD.md` section 4, so a rule is held by the stage that can close its proof rather than by the document it was extracted from. Requires an agreed derivation basis rather than a positional heuristic; recompute per-stage coverage afterwards and revalidate any `SATISFIED` row whose owning stage changes.
- Return-to after `MD-DEP-0004` closes: continue `MD-B01` coverage against the corrected 170-rule denominator, then `MD-B02` (deferred, not skipped), then `MD-B03` closure under the revised prerequisites.
- Note on denominators: every stage coverage cell above was recomputed from the post-re-derivation matrix. Figures cited in evidence issued before `MD-B01-A004` were taken against the 1407-rule denominator and remain true as issued.

> **Environment note — RESOLVED by execution.** MariaDB at `127.0.0.1:3306` was unreachable at `MD-B00-A002` closure, so the suite then reported 5 errors and 16 skipped. The server is back (10.4.27-MariaDB) and every deferred database proof has been executed. The suite returns exactly the pre-outage figures — 1537 tests, 4 errors, 67 failures, 0 skipped — confirming the outage was the sole cause and the `E-MD-B00-A002-001` control figures were accurate. The 80 previously-skipped database-dependent tests all pass, including 10 live-corpus invariants. Executing the deferred drift detection also exposed a defect in the detector itself, fixed and mutation-proven under the open `MD-B03-A001`; see `E-MD-B03-A001-002`.

### MD-B01 current proof retained pending revalidation

Latest attempt `MD-B01-A003`, baseline `MD-B01-A003-BL001`, evidence `E-MD-B01-A003-001`. Prior attempts `MD-B01-A001` and `MD-B01-A002`.

Done at `MD-B01-A001`: the one `MD-B01`-owned dead documentation path was rebound and guarded, `TerminologyOwnerVocabularyTest` returned to 7/7, and 22 new executable checks were added covering scope drift, the four time boundaries, activation semantics, and policy-vocabulary leakage across tables, columns, status enums, reason codes, command signatures, and config keys. Seven mutations confirmed each guard fails closed. Five rules reached `SATISFIED`.

Done at `MD-B01-A002`: 16 further checks covering date-driven capability and provider limitation abstraction, plus boundary-direction and alias-containment guards. Provider transport shape is proven absent from `app/Domain` and `app/Application` and present in exactly one adapter; the acquisition port is provider-neutral and date-addressed; 123 market-data files carry zero policy-namespace imports; the bare `eligible` column exists on exactly two tables. Eight mutations confirmed each guard fails closed. Eleven more rules reached `SATISFIED`, bringing coverage to `16/127`.

Done at `MD-B01-A003`: 8 checks on the locked interpretation rules governing what the platform may claim about itself. Three are behavioural — they seed a genuinely readable publication and call `MarketDataReadinessService`, because a freshness claim is made by the readiness payload rather than by a document. A ready date with no governed marker reports `is_ready` true and `activation_state` `DEVELOPMENT`; the blocked path answers in the same vocabulary; setting the marker moves the state and only for dates at or after it. No executable surface makes a consecutive-SLO claim, no product term is aliased to another, the eligibility surface carries no approval or ranking vocabulary, and nothing asserts `decision-grade` across code, reason codes, command names, test names, or 50+ active documents. Eight mutations confirmed each guard fails closed, each verified as applied first. Five more rules reached `SATISFIED`, bringing coverage to `21/127`.

Also raised at `MD-B01-A003`: `F-MD-B01-A003-001` (P2). Ten frozen strategy contracts use the bare word `eligible` without repeating its data-usability meaning, which `Domain_Boundary_Invariants_LOCKED.md` calls the only thing preventing the misreading. `MD-S020-R0067` is therefore not satisfiable by implementation work — its subject is the wording of frozen strategy.

Not done, and why: `F-MD-B01-A001-001` establishes that 15 of the 127 required rules are bare colon-terminated list headers and 3 more are owner pointers, so the current `127/127` target cannot be reached by evidence as presently classified. At the time of `MD-B01-A001..A003`, no matrix row was reclassified. `DOC-CHG-20260821-001` now clarifies that correcting `STRATEGY_TO_IMPLEMENTATION_TRACEABILITY_MATRIX.csv` is legitimate `MUTABLE_TRACEABLE` governance work when strategy semantics are preserved. The finding and `MD-DEP-0004` remain OPEN until the matrix is actually re-derived by predicate meaning/proof ownership and affected verification is invalidated/rebaselined/revalidated as required.

**Predicate half resolved at `MD-B01-A004`.** The earlier scan found 210 of 1407 required rules were non-predicate list headers, affecting 17 of 18 stages. The governed re-derivation demoted 214 non-predicates and promoted 817 predicate-bearing children, moving the required set from 1407 to 2010 — a 43% increase, not a reduction. Non-predicate required rows are now **0** and **all 18 stages** can reach full required coverage. What still blocks closure is the stage-ownership half of `MD-DEP-0004`: `primary_stage` is still derived from the document a rule was extracted from, so a stage may hold rules it cannot prove.

Do-not-repeat, `MD-B01-A001`: the first policy vocabulary missed `ranked_picks`, one of the boundary contract's own forbidden examples, because a bare `rank` alternative does not match `ranked`. A naming guard is only as good as its word list, so the word list is now asserted against the contract's example lists in both directions rather than trusted.

Do-not-repeat, `MD-B01-A002`: two of them. First, a mutation that does not mutate is indistinguishable from a guard that does not guard — one mutation targeted a `use Illuminate` line the file did not contain, reported "guard did not react", and was a no-op; verify the mutation landed before concluding anything about the guard. Second, absence is only evidence when paired with a positive locator: every "X is not in this layer" check is paired with "X is in the adapter", so a rename cannot turn the suite green by making the search find nothing anywhere.

Do-not-repeat, `MD-B01-A003`: a scan is only as good as the pattern behind it. The alias-repetition scan first matched only snake_case `data_usable` and reported 18 non-conforming documents; seven of those state the meaning in prose as `data-usability`, which satisfies the rule as written, and the true figure is 11. A finding published from the first number would have named seven documents as defective when they are not. Check what a pattern misses before you count with it — and when a guard flags its own test method, rename the method rather than adding a self-exemption, because the carve-out weakens the guard for every other file.

## MD-B03-A001 — dependency remediation (out-of-order entry)

`MD-B03` was entered before `MD-B01` closed and before `MD-B02` opened. The basis is recorded in `MD-B03-A001-BL001`: `MD-DEP-0001` and `MD-DEP-0002` were registered `OPEN_BLOCKING` with owner `MD-B03`. Those two dependencies are now `RESOLVED`. The earlier statement that `MD-DEP-0004` was unfixable merely because its matrix lives under `authority/` is superseded by `DOC-CHG-20260821-001`: the traceability matrix is `MUTABLE_TRACEABLE` and may be corrected through governed execution without changing strategy semantics. That clarification does not itself close `MD-DEP-0004`; the semantic matrix correction still has to be executed and revalidated. `MD-B01` remains `IN_PROGRESS` at the current snapshot of `21/127`; `MD-B02` remains deferred, not skipped.

Seven defects fixed, each proven by execution rather than by path resolution. Clean install now runs 62/62 migrations to exit 0 on an isolated database; the reason-code seeder brings `eod_reason_codes` from 43 to 436 with zero missing; the schema drift detector is back online at 30 tests / 721 assertions and shows the SQLite mirror genuinely matches the canonical schema.

Fixing the migration path exposed two further P0 defects that only execution could reveal. `hasIndex()` in `2026_06_04_000001` called `getDoctrineSchemaManager()` and swallowed every Throwable as "index absent" — `doctrine/dbal` is not a dependency, so the guard always answered false and the migration re-added an index the clean-install base already creates. A second unguarded index block in `2026_08_08_000001` failed the same way. Both fixed.

Do-not-repeat: a guard that answers "absent" when it cannot determine presence is worse than no guard, because it looks like protection. That is now the third instance of this exact shape in this codebase, after the reason-code seed guard and the relationship gate self-test. When a probe cannot answer, it must raise, not guess.

## Scope of the prior MD-B00-A001 closure

`SC-MD-B00-A001-001` remains an immutable record of what `MD-B00-A001` closed under the governance in force when it was issued. `DOC-CHG-20260821-001` subsequently strengthened closure/gate invariants, so the current `MD-B00` stage is re-opened for governance revalidation; the prior closure record is not rewritten and is not treated as sufficient current closure. It certifies the recorded baseline facts of that attempt, not implementation correctness under the revised governance.

At that closure the PHPUnit suite is `RED` (1488 tests, 26 errors, 108 failures), clean install cannot pass migration 3 of 62, and the reason-code seeder throws. Those are recorded baseline facts owned by `MD-B03`, `MD-B19`, and `MD-B21`, not defects in `MD-B00`. All 1407 required strategy rules remain `NOT_ASSESSED` and every existing artifact remains `NOT_ASSESSED_REVALIDATION_REQUIRED`.


# Command Surface Safety Inventory

Status: **V2 RE-AUDITED / NOT RELOCKED**. Historical command-output proof remains evidence only for the behavior that actually ran. This inventory is **not** behavioral authority. In addition to prohibited price-repair/price-derived verification apply paths, every source/master command below that performs overwrite/upsert correction of temporal source truth is a V2 migration gap until it writes immutable observation/revision rows or closes/opens temporal revisions without rewriting accepted history.

| Command | Type | State Changing | Destructive | Guard | Dry-run / Apply | Idempotency | Reason Code Output | Runtime Proof | Gap / Action |
|---|---|---:|---:|---|---|---|---|---|---|
| `market-data:daily` | import pipeline | Yes | No | date/source validation | Not required; import-only | Same requested date/source context must not corrupt final state | run/final/source reason code via run summary | Ops command test + pipeline tests | Keep import-only; no readable publish claim |
| `market-data:eod-bars:ingest` | stage command | Yes | No | date/source/request-mode validation | Not required | run_id/requested date guarded by pipeline; explicit full-publish stage chain requires `--request_mode=full_publish` | run/final/source reason code via run summary; invalid mode returns `COMMAND_INVALID_REQUEST_MODE` | Pipeline/service tests + ops runtime matrix | Keep stage summary complete |
| `market-data:eod-indicators:compute` | stage command | Yes | No | date/source validation | Not required | run_id/requested date guarded by pipeline | run/final reason code via run summary | Pipeline/service tests | Keep deterministic artifact behavior |
| `market-data:eod-indicators:recompute-current` | current-publication indicator recompute | Yes | Yes, current replace through correction-current lifecycle | start/end date validation; current readable publication required; force reason guard; `--max_dates` must be a positive integer | `--dry-run` non-mutating and preflights each date; `--max_dates` bounds how many publications one run may replace; normal execution requires `--force_replace_reason` | Same current publication can be recomputed through correction-current; unchanged candidate cancels safely | correction/current run reason codes; source/bar/import flags false | `RecomputeCurrentIndicatorsCommandTest` + command surface static guard + full-range runtime/evidence replay proven | Use when OHLCV/source/master are already correct and only publication-bound indicators must be recomputed; no source/master/eod_bars writes |
| `market-data:eod-eligibility:build` | stage command | Yes | No | date/source validation | Not required | run_id/requested date guarded by pipeline | run/final/coverage reason code via run summary | Pipeline/service tests | Keep coverage context visible |
| `market-data:audit:hash` | stage command | Yes | No | date/source validation | Not required | content hash must be deterministic | run/final reason code via run summary | Hash/service tests | Keep artifact hashes stable |
| `market-data:dataset:seal` | stage command | Yes | No | date/source validation | Not required | seal rerun must be safe for same artifact | run/final/seal context via run summary | Pipeline/finalize tests | Do not mark readable without finalize |
| `market-data:run:finalize` | finalize / pointer gate | Yes | Yes, pointer switch | date/source validation + pipeline pointer validation | Not separate; finalize service owns pointer guard | Required for same run/publication context | run/final/coverage/pointer reason code via run summary | Finalize/pointer tests | Force must never bypass pointer contract |
| `market-data:promote` | publish / coverage gate | Yes | Yes, current replace | date/source/mode validation + force reason guard | `force_replace=false` by default; force requires reason | Required for same candidate | run/final/coverage/source reason code via run summary | Ops + pipeline tests | Promote must enforce coverage gate |
| `market-data:backfill` | import backfill | Yes | No | start/end/source validation | Not required; import-only | Date range repeat must not corrupt publications | case-level reason/source context | Backfill service tests | Keep cases reason-coded |
| `market-data:backfill:lifecycle` | full lifecycle range orchestration | Yes | Yes, promotes/finalizes current publications through guarded pipeline | start/end/source validation; plan/diagnose/resume guards; lifecycle orchestrator ownership | `--plan` is non-mutating; normal execution must be explicit; evidence/replay flags explicit | Date checkpoints are resumable; same completed date should not be reprocessed unless policy allows | source acquisition, promote, evidence, replay, and checkpoint reason codes | API backfill lifecycle static/unit tests + operator runtime proof | Use for range lifecycle, not as a raw import shortcut |
| `market-data:backfill:missing-tickers` | missing ticker lifecycle range orchestration | Yes | Yes, promotes/finalizes current publications through guarded pipeline | start/end/source validation; `source_mode=api`; ticker-master/current-bars gap scan; optional `--ticker_codes` | `--plan` is non-mutating; normal execution must be explicit; evidence/replay flags explicit | Same missing ticker/date can be rerun safely through candidate/full lifecycle; no-op when no current bar gap exists | missing ticker counts, source acquisition, promote, evidence, replay, and lifecycle reason codes | API backfill lifecycle static guard + command/help/plan proof | Use for selected missing ticker/date gaps; do not use unfinished duplicate runs as the gap source |
| `market-data:evidence:export` | evidence export | No DB mutation except artifacts | Artifact overwrite possible | exactly-one selector + trade-date validation | Output artifact writes are explicit by `--output_dir` | Same selector must be repeatable | command blocked/execution reason code | Evidence export tests | No latest replay evidence without trade_date |
| `market-data:evidence-replay:full-range-current` | current-publication proof orchestration | Yes, replay proof rows; no market-data publication mutation | Artifact overwrite possible | optional start/end date validation; current pointer/readable publication required per trading date | Output artifact writes are explicit by `--output_dir`; `--continue_on_error` is explicit | Same current run/publication fixture should MATCH/PASS repeatedly except generated fixture timestamp and new replay ids | `NO_READABLE_PUBLICATION`, replay mismatch, evidence admission state, command blocked/domain reason code | Full-range current evidence/replay service + command tests | Use for proof-only full-range evidence/replay; do not re-import or promote OHLC |
| `market-data:sector-indexes:ingest-api` | sector index API bar import | Yes; **legacy runtime currently upserts** sector benchmark OHLC with `--apply` (V2 NONCONFORMANT for accepted history) | No publication/pointer mutation; **legacy benchmark overwrite/upsert must migrate to immutable observation + canonical revision/publication flow** | start/end trading-date validation; active sector index + seeded benchmark validation; provider/symbol mapping; conflicting dry-run/apply blocked | Default `DRY_RUN`; `APPLIED` only with `--apply` | Legacy same-key upsert is operationally repeatable but is **not V2 immutability**; target appends observations/revisions and deterministically selects the canonical revision | `COMMAND_DRY_RUN_ONLY`, `COMMAND_APPLY_CONFIRMED`, `SECTOR_INDEX_API_INGEST_INCOMPLETE`, provider/source reason codes | Sector index API import command tests | Validate provider symbols first; after apply, run the existing lifecycle/promote flow for affected dates to stamp sector rotation fields |
| `market-data:sector-indexes:import-bars` | sector index bar import | Yes; **legacy runtime currently upserts** sector benchmark OHLC with `--apply` (V2 NONCONFORMANT for accepted history) | No publication/pointer mutation; **legacy benchmark overwrite/upsert must migrate to immutable observation + canonical revision/publication flow** | input CSV exists; required headers; active sector index + seeded benchmark validation; conflicting dry-run/apply blocked | Default `DRY_RUN`; `APPLIED` only with `--apply` | Legacy same-key upsert is operationally repeatable but is **not V2 immutability**; target appends observations/revisions and deterministically selects the canonical revision | `COMMAND_DRY_RUN_ONLY`, `COMMAND_APPLY_CONFIRMED`, validation errors | Sector index import command tests | Import sector index history first, then run the existing lifecycle/promote flow for affected dates to stamp sector rotation fields |
| `market-data:sectors:import-memberships` | authoritative temporal sector membership import | Yes; `--apply` appends immutable membership revisions and, for reclassification, appends a closure revision plus the new interval | No publication/pointer mutation; prior accepted membership rows are never edited | CSV requires stable `listing_uid`, IDX-IC sector/effective interval, explicit source/ref/authority class, and `recorded_at`; ticker is optional cross-check only; `DERIVED_REFERENCE` cannot establish membership; governed operator input requires operator/reason; dry-run/apply both validate overlap at every known-time checkpoint | Default `DRY_RUN`; `APPLIED` only with `--apply`; failed postflight rolls the transaction back | Same authoritative fact is a no-op; conflicting effective/known identity and temporal overlap fail closed; reclassification preserves as-known reconstruction | `COMMAND_DRY_RUN_ONLY`, `COMMAND_APPLY_CONFIRMED`, provenance/identity/overlap validation errors | Sector import command + repository effective/as-known tests | Obtain and retain the official IDX artifact/reference before claiming exchange-authoritative coverage; then import membership and run the existing governed lifecycle/correction flow for affected publication dates |
| `market-data:events:import-corporate-actions` | corporate-action source import | Yes; **legacy runtime currently upserts** corporate-action source rows with `--apply` (V2 NONCONFORMANT for accepted event history) | No publication/pointer mutation; target appends event/source revisions and never mutates an event/factor revision already bound to a publication | input CSV exists; required headers; ticker master validation; duplicate CSV identity blocked; conflicting dry-run/apply blocked | Default `DRY_RUN`; `APPLIED` only with `--apply` | Legacy same-key upsert is not V2 immutability; target uses stable event/listing identity plus append-only source/event revisions | `COMMAND_DRY_RUN_ONLY`, `COMMAND_APPLY_CONFIRMED`, validation errors | Corporate action import command + repository tests | Import source context first, then run the existing lifecycle/promote flow for affected dates to stamp nullable event-risk fields |
| `market-data:events:record-authoritative-terms` | authoritative corporate-action revision/evidence record | Yes; `--apply` appends only event revisions and immutable capture/accepted source observations | No legacy event, factor, run, publication, pointer, bar/history, indicator, eligibility, or lineage-binding mutation | exact-schema JSON manifest; declared `record_only=true` scope; single as-of listing; governed STOCK_SPLIT semantics; exact KSEI/CSD HTTPS host/path; complete ordered dates and internally consistent ratio/nominal terms; conflicting duplicate revision blocked; before a new insert, HTTPS fetch must return HTTP 200 PDF bytes whose length/SHA-256 exactly match the declaration | Default `DRY_RUN`; `APPLIED` only with `--apply`; `--dry-run` and `--apply` conflict; dry-run does not fetch remote bytes | Same event/revision and identical stored evidence is a true no-op without remote dependency; content conflict requires a new governed append-only revision and never overwrites | `COMMAND_DRY_RUN_ONLY`, `COMMAND_APPLY_CONFIRMED`, `AUTHORITATIVE_TERMS_VALIDATED`, fail-closed fetch/hash/length/content-type/validation errors | `RecordAuthoritativeCorporateActionTermsCommandTest` + production remote-byte/re-apply/residue oracle | Stage 6 record-only surface. Do not create/apply factors, rebuild publications, or treat declared scope as full-range event completeness; factor/product application remains a separate governed lifecycle |
| `market-data:market-structure:record-authoritative-rules` | authoritative IDX market-structure revision/evidence record | Yes; `--apply` appends only market-structure revisions, tier children, and immutable capture/accepted source observations | No run, publication, pointer, bar/history, indicator, eligibility, factor, event, or lineage-binding mutation | exact-schema JSON; `record_only=true`; locked dataset start; four contiguous price-band regimes plus full-range floor/tick coverage; explicit standard-board scope and exclusions; unknown board fail-closed; pinned HTTPS hosts/transport roles; verified response HTTP status, content type, exact hash/ref/byte length, schema fingerprint, and bounded capture sample; immutable identity/conflict checks | Default `DRY_RUN`; `APPLIED` only with `--apply`; conflicting mode blocked; dry-run never fetches remote bytes | Identical current revisions/evidence are a true network-independent no-op; legacy evidence mismatch appends a superseding correction revision/observation pair; semantic conflict is blocked; no old row is overwritten or deleted | `COMMAND_DRY_RUN_ONLY`, `COMMAND_APPLY_CONFIRMED`, `AUTHORITATIVE_MARKET_STRUCTURE_VALIDATED`, fail-closed fetch/hash/length/content-type/schema/sample/coverage/revision errors | `RecordAuthoritativeExchangeMarketStructureCommandTest` + production remote-byte/re-apply/residue oracle | Stage 7 (`F-011a`) record-only surface. Resolution and application to series remain exclusively Stage 8 (`F-011b`) |
| `market-data:trading-status:record-authoritative-snapshot` | Stage 8 authoritative IDX status evidence record | Yes; `--apply` appends exactly two capture/accepted observation pairs and the 59 source-backed status revisions declared by the governed manifest | No legacy status event, run, publication, pointer, bar/history, indicator, eligibility, factor, market-structure, or admission mutation | exact-schema local manifest; official IDX HTTPS hosts; exact 2026-06-30 long-suspension table and bounded transition search through 2026-07-28; HTTP 200/content-type/hash/length/schema/sample proof; stable listing mapping; full-session/source-revision binding | Default or `--dry-run` validates local scope and is read-only without remote fetch; `--apply` fetches authority bytes before its transaction; conflicting mode blocked | An exact completed scope is a network-independent no-op; partial/conflicting revision scopes fail closed; accepted evidence and revisions are append-only | `AUTHORITATIVE_TRADING_STATUS_VALIDATED`, `AUTHORITATIVE_TRADING_STATUS_TRANSITIONS_VALIDATED`, registered `STAGE8_STATUS_*` failures | focused status/coverage tests + production apply/re-apply/residue oracle | Stage 8 evidence only; reported older suspension dates are retained as source evidence and never projected backwards |
| `market-data:corpus:admit-conformant-suffix` | Stage 8 conformant-corpus admission measurement | Yes; `--apply` appends one immutable admission decision only | No source acquisition, status/master projection, run, publication, pointer, artifact, factor, replay, or campaign-attempt mutation | completed frozen Stage 8 acquisition cache; exact accepted status evidence; already-complete temporal projection; locked 0.98 denominator/quality gates on every date; earliest continuous passing suffix; hashed measurement/status/observation sets | Default or `--dry-run` is a strictly read-only measurement; `--apply` appends the decision; conflicting mode blocked | Same evidence produces the same decision UID and returns `ALREADY_ADMITTED`; a different active decision or changed immutable evidence fails closed | `STAGE8_CONFORMANT_SUFFIX_ADMITTED`, `STAGE8_ADMISSION_EVIDENCE_INVALID`, `STAGE8_ADMISSION_SUFFIX_NOT_FOUND` | admission service tests + production plan/apply/re-apply oracle | Preserves intentional dataset start 2023-01-02; admission is a separate consumer-read boundary and Stage 9 remains unexecuted |
| `market-data:corpus:reconstruct-current` | bounded Stage 8 current-corpus lifecycle reconstruction | Yes; `--apply` creates immutable observations, corrections, runs, candidate artifacts, factor/market-structure bindings, sealed publications, and guarded pointer switches | Yes, current pointers move only through normal correction/finalize gates; frozen baseline rows remain immutable | exact current-pointer/calendar scope; sealed-success baseline and three artifact hashes; complete temporal identity projection; one bounded Yahoo range acquisition; per-date coverage/hash/seal/finalize guards; frozen baseline snapshot hashes; final zero-violation oracle | Default or `--dry-run` is read-only plan; mutation requires `--apply`; `--resume` requires `--apply`; `--dry-run` and `--apply` conflict | One campaign freezes every target in its exact scope; successful targets checkpoint once; acquisition is cached; crash after pointer switch is recovered from correction lineage; unfinished campaign must resume; completed campaign is re-audited | registered `STAGE8_*`, factor hold/apply, market-structure fail-closed, identity/source/pipeline reason codes | Stage 8 governance tests + full MarketData suite + production plan/oracle | Stage 8 only. It must not invoke fixture generation, replay, Stage 9 proof, direct legacy relabel, or history rewrite |
| `market-data:events:import-trading-status` | trading-status/UMA/suspend/special-monitoring source import | Yes; **legacy runtime currently upserts** trading-status source rows with `--apply` (V2 NONCONFORMANT for temporal source truth) | No publication/pointer mutation; target appends source/status revisions and resolves effective intervals/full-session evidence point-in-time | input CSV exists; required headers; ticker master validation; boolean validation; duplicate CSV identity blocked; conflicting dry-run/apply blocked | Default `DRY_RUN`; `APPLIED` only with `--apply` | Legacy same-key upsert is not V2 temporal revision semantics; target binds stable `listing_id`, source observation/revision, known time, and effective interval | `COMMAND_DRY_RUN_ONLY`, `COMMAND_APPLY_CONFIRMED`, validation errors | Trading status import command + repository tests | Import immutable source/status revision context first; the dictionary may resolve risk family/transition class, but **expected-bar state is resolved separately from point-in-time listing + session + effective status/full-session evidence**; then run governed correction/lifecycle flow for affected publication dates |
| `market-data:detect-price-scale-breaks` | anomaly detection | Yes, candidate evidence rows only with explicit apply | No canonical/factor/publication mutation permitted | bounded date/listing scope and versioned detector | dry-run or candidate-evidence write only | repeat creates/reconciles detector evidence without changing facts | detection/classification reasons | corrected detector contract; new runtime proof required | May quarantine/contaminate only; cannot verify actions or activate factors |
| `market-data:repair-price-scale-stretches` | **PROHIBITED legacy surface** | Current code can rewrite bars/history | **Yes—P0 violation** | no guard can legitimize in-place history rewrite | `--apply` must be removed/disabled | not applicable | attempted use must be blocked | legacy execution proof is not admissible | Remove registration/service apply path; correction must create a new immutable publication |
| `market-data:events:derive-corporate-actions` | detector-candidate support only | Current apply behavior is prohibited from creating/updating verified event/factor state | yes if it changes adjustment authority | price behavior is never verification | apply must be disabled until it only writes unverified candidates | candidate identity only | unverified/contamination reasons | legacy derived-factor proof is superseded | Verified event revision requires authoritative/manual evidence and separate factor lifecycle |
| `market-data:replay:verify` | replay proof | Yes, replay result rows | Artifact write optional | positive run_id + service fixture proof | Output evidence export explicit by `--output_dir` | Same fixture/context must be repeatable | replay mismatch/failure reason code | Replay verification tests | No volatile expected proof |
| `market-data:replay:smoke` | replay suite | Yes, replay proof rows | Artifact write optional | run_id through service contract | Output artifact explicit by `--output_dir` | Built-in cases repeatable | case mismatch reason code | Replay smoke tests | Keep case identifiers visible |
| `market-data:replay:backfill` | replay backfill | Yes, replay proof rows | Artifact write optional | date validation | Output artifact explicit by `--output_dir` | Same fixture/date range repeatable | case mismatch/error reason code | Replay backfill tests | Keep fixture path visible |
| `market-data:replay:fixture:generate` | replay fixture generation | No DB mutation; writes fixture artifacts | Artifact overwrite possible | positive run_id + output_dir/case validation | Output artifact explicit by `--output_dir` | Same run should regenerate same deterministic expected proof except fixture_created_at | command blocked/domain reason code | Replay fixture generation command test + static guard | Use generated fixture for runtime MATCH proof, not stale committed valid_case |
| `market-data:session-snapshot` | supplemental snapshot capture | Yes | Slot overwrite by replace | date/source validation + readable publication service guard | Not separate; replace is slot-scoped and deterministic | Same slot/date replaces same scope deterministically | snapshot partial reason code via service events | Session snapshot service tests | Keep readable current publication guard |
| `market-data:session-snapshot:purge` | retention purge | Yes | Yes, delete rows | `--apply` required for mutation | Default `DRY_RUN`; `APPLIED` only with `--apply` | Same cutoff after apply should become no-op/candidate 0 | `COMMAND_DRY_RUN_ONLY` / `COMMAND_APPLY_CONFIRMED` | Command + service + static guard tests | Patched: dry-run counts candidates without delete |
| `market-data:correction:request` | correction lifecycle | Yes | No | trade_date/reason_code required + date validation + current sealed readable PASS baseline lookup | Not required | Duplicate policy owned by repository | command input reason code + request reason code + `CORRECTION_BASELINE_LINK_MISSING` | Correction command tests + runtime correction proof | Keep request reason and baseline publication visible |
| `market-data:correction:approve` | correction lifecycle | Yes | No | positive correction_id + repository strict status guard | Not required | Only `REQUESTED` is approvable; re-approve/rerun approval states are blocked | `COMMAND_CORRECTION_NOT_FOUND`, `COMMAND_CORRECTION_STATUS_NOT_APPROVABLE` | Correction command/repository tests | Keep status visible; do not approve `APPROVED`, `REPAIR_EXECUTED`, `CONSUMED_CURRENT`, or terminal states |
| `market-data:correction:run` | correction pipeline | Yes | Yes, candidate/current switch through finalize | correction status guard + date/source validation | Not separate; correction service owns baseline guard | Terminal correction rerun must be blocked/skipped by lifecycle | command correction reason code + run reason code | Correction lifecycle tests | Preserve baseline/current pointer |
| `market-data:current-publication:repair` | integrity repair | Yes | Yes, clears invalid current state | `--apply` required for mutation; apply also requires `--reason` or `--force_reason` | Default dry-run; applies only after review and reason | Same invalid state clear is idempotent after first apply | `COMMAND_DRY_RUN_ONLY` / `COMMAND_APPLY_CONFIRMED` / `COMMAND_DESTRUCTIVE_GUARD_REQUIRED` + integrity reasons | Static guard + command tests + repair help/runtime guard | Keep repair narrow; no valid pointer rewrite; record pointer before/after |
| `market-data:provider:smoke` | live provider smoke | No DB mutation; writes command-output artifact only | No | single ticker required; full-universe/multi-ticker blocked; provider option overrides adapter config; JSON option emits JSON stdout | Always dry-run; publication/seal/finalize/pointer/full-universe flags must remain false | Repeat may be rate-limited by provider and must not be counted as PASS unless `PROVIDER_SMOKE_OK` | `PROVIDER_SMOKE_OK`, `PROVIDER_RATE_LIMITED`, `PROVIDER_TIMEOUT`, `PROVIDER_NETWORK_ERROR`, `PROVIDER_RESPONSE_PARSE_FAILED`, `PROVIDER_EMPTY_OR_INVALID_RESPONSE`, `PROVIDER_TRADE_DATE_NOT_FOUND_IN_RESPONSE`, ticker/full-universe guards | Provider smoke static guard + command-output artifact | Runtime proof is BLOCKED when provider returns rate limit/timeout/network/parse/empty/invalid-date response; do not claim provider PASS without `returned_row_count >= 1` and `PROVIDER_SMOKE_OK` |

### V2 source/master command admission rule

Rows describing `upsert` are **observed legacy runtime behavior only**. A source/master command is not V2-conformant merely because it is dry-run guarded or idempotent. To be admitted under the new strategy it must:

1. persist immutable source observations before canonicalization;
2. bind stable `listing_id`/temporal mapping rather than current ticker identity;
3. append a new temporal/event/factor/canonical revision when accepted truth changes;
4. preserve known/effective timestamps and source evidence;
5. force affected analytical/publication artifacts through governed correction/republication; and
6. never overwrite a revision already bound to sealed history.

## Final policy

Destructive command behavior is allowed only for explicitly eligible non-authoritative retention artifacts or atomic publication-pointer lifecycle actions. No dry-run/apply/force/reason guard can authorize in-place observation, canonical, factor, snapshot, manifest, seal, or published-history mutation. Price repair apply and price-derived verification apply must be removed/disabled. Force-like promotion remains default-off and cannot bypass candidate completeness, lineage, config, hashes, seal, or coherent-product gates. Invalid input returns `BLOCKED` with registered reasons.

Commands with logically required arguments may expose those arguments as optional at the Symfony parser layer only when that is required to produce command-owned `status=BLOCKED` and `reason_code=COMMAND_MISSING_REQUIRED_INPUT` output. The operator contract remains required-input: docs/runbooks must continue to name the required values, and the command must not fall through to a raw framework missing-argument error.


## Validation

- 2026-05-07 operator-local PASS: `CommandSurfaceSafetyStaticGuardTest.php` -> OK (5 tests, 81 assertions).
- 2026-05-07 operator-local PASS: `SessionSnapshotServiceTest.php` -> OK (6 tests, 38 assertions).
- 2026-05-07 operator-local PASS: `vendor/bin/phpunit tests/Unit/MarketData --filter "CommandSurface"` -> OK (47 tests, 348 assertions).
- 2026-05-07 operator-local PASS: `vendor/bin/phpunit tests/Unit/MarketData --filter "DryRun"` -> OK (2 tests, 15 assertions).
- 2026-05-07 operator-local PASS: `vendor/bin/phpunit tests/Unit/MarketData --filter "Apply"` -> OK (4 tests, 26 assertions).
- 2026-05-07 operator-local PASS: full `vendor/bin/phpunit tests/Unit/MarketData` -> OK (312 tests, 3899 assertions).
- 2026-05-20 local PASS: `CommandSurfaceSafetyStaticGuardTest.php` -> OK (5 tests, 89 assertions); `OpsCommandSurfaceTest.php --filter "current_publication_repair"` -> OK (2 tests, 12 assertions); repair apply without reason blocked with `COMMAND_DESTRUCTIVE_GUARD_REQUIRED`.
- 2026-05-20 ops runtime matrix LOCKED: all 20 historical public market-data commands were registered and help-rendered with exit 0; invalid/missing input proof was command-owned and reason-coded; seeded runtime proof passed for finalize re-run, run/replay/correction evidence export, replay fixture generation, replay verify/smoke/backfill, repair dry-run/apply guard, purge dry-run/apply-zero, no-readable snapshot block, correction lifecycle blocks, and promote force guard; production-ready fixture proof passed for fresh daily/backfill/promote/stage success, real lock conflict, held/not-readable, failed source, repair apply invalid pointer, session snapshot success, evidence export, and replay verify/smoke/backfill. Full matrix is recorded in `docs/market_data/audit/OPS_COMMAND_SURFACE_RUNTIME_MATRIX_INVENTORY.md`.
- 2026-05-23 final reconciliation: current public market-data command surface was 21 commands after `market-data:provider:smoke`; provider-smoke safe-mode surface is enforced, `--json` emits JSON stdout, `--provider` overrides `market_data.source.api.provider`, and the final embedded live provider runtime proof is `provider_smoke_status=PASS` / `reason_code=PROVIDER_SMOKE_OK` / `http_status=200` with all non-destructive safety flags false.
- 2026-06-03 command surface extension: current public market-data command surface is 26 commands after including the already-public `market-data:backfill:lifecycle`, adding `market-data:evidence-replay:full-range-current` for proof-only full-range current publication evidence/replay, adding `market-data:sectors:import-memberships` for dry-run/apply guarded sector membership import, adding `market-data:sector-indexes:import-bars` for dry-run/apply guarded sector index bar import, and adding `market-data:sector-indexes:ingest-api` for dry-run/apply guarded sector index API import. The evidence/replay command does not fetch API data, import OHLC, promote, finalize, or switch current publication pointers; it exports run evidence, generates a runtime fixture per current publication, verifies replay, exports replay evidence, and writes a summary artifact. Historical runtime fact: the sector commands upserted source/membership or benchmark rows after validation/apply. **This behavior is superseded as a V2 target and is not admitted as immutability proof.**
- 2026-06-04 event-risk source context extension: current public market-data command surface is 28 commands after adding `market-data:events:import-corporate-actions` and `market-data:events:import-trading-status`. Historical runtime fact: these commands upserted source context rows after CSV validation/apply; **that overwrite semantics is superseded as a V2 target**; they do not import OHLC, recompute indicators, promote, finalize, or switch current publication pointers.
- 2026-06-04 missing-ticker lifecycle extension: public market-data command surface reached 29 commands after adding `market-data:backfill:missing-tickers`. The command scans ticker-master/current-bar gaps, supports `--ticker_codes`, uses API range acquisition for missing tickers only, and then runs the normal lifecycle so evidence/replay can prove the resulting current publication.
- 2026-06-06 current indicator recompute extension: current public market-data command surface is 30 commands after adding `market-data:eod-indicators:recompute-current`. The command recomputes publication-bound indicators from existing current readable bars through correction-current lifecycle without source acquisition, bar ingest, source/master writes, or `eod_bars` writes.
- 2026-05-20 post-ledger validation PASS: `OpsCommandSurfaceTest.php` OK (57 tests, 341 assertions), `CommandSurfaceSafetyStaticGuardTest.php` OK (5 tests, 89 assertions), `OpsCommandSurfaceRuntimeMatrixStaticGuardTest.php` OK (6 tests, 114 assertions), `Command` filter OK (97 tests, 1009 assertions), `Ops` filter OK (74 tests, 616 assertions), `StaticGuard` filter OK (176 tests, 4124 assertions), and full `tests/Unit/MarketData` OK (475 tests, 6942 assertions).
- 2026-07-31 price-continuity extension: current public market-data command surface is 33 commands after adding `market-data:detect-price-scale-breaks`, `market-data:repair-price-scale-stretches`, and `market-data:events:derive-corporate-actions`. Every count recorded above this line is the surface as it stood on that date and is left unedited; entries stating a "current" surface of 20 through 30 describe earlier states, not this one.
- 2026-07-31 gap closed: those three commands were registered in the console kernel and absent from the table above for the whole of their existence, because `CommandSurfaceSafetyStaticGuardTest` walked a hand-written roster of thirty command names rather than the kernel. A command missing from a roster is a command the roster cannot report. The check is now derived from the kernel, so an unlisted command fails the suite instead of passing it. Command counts stated in prose are therefore no longer the control: the derived check is.

## 2026-05-23 — Final Provider Smoke / Full PHPUnit PASS Document Reconciliation

[SESSION] FINAL_PROVIDER_SMOKE_FULL_PHPUNIT_DOC_SYNC

[SESSION_STATUS] OPS_RUNTIME_PARITY_PASSED

[FINAL_DECISION]
- Current source ZIP is documented as `OPS_RUNTIME_PARITY_PASSED`.
- Final provider smoke is `FINAL_PROVIDER_SMOKE=PASSED` and `LIVE_PROVIDER_SMOKE_PASSED`.
- Authoritative provider-smoke artifact: `storage/app/market-data/provider-smoke-safe-mode/command-output/provider-smoke-bbca.txt`.
- Provider smoke proof: `provider_smoke_status=PASS`, `reason_code=PROVIDER_SMOKE_OK`, `source_reason_code=none`, `http_status=200`, `returned_row_count=1`, `attempt_count=1`, `retry_max=0`, `retry_exhausted=false`.
- Safety proof: `publication_created=false`, `seal_executed=false`, `finalize_executed=false`, `pointer_switched=false`, `readable_publication_created=false`, `full_universe_fetch=false`.
- Scheduler due-run runtime proof remains present and no silent scheduler failure is claimed.
- Final targeted validation passed: `OpsCommandSurfaceRuntimeMatrixStaticGuardTest` -> OK (6 tests, 120 assertions).
- Final full validation passed: `vendor\bin\phpunit tests/Unit/MarketData` -> OK (492 tests, 7590 assertions), Time 00:36.861, Memory 40.00 MB.

[RECONCILIATION]
- Earlier wording that described provider smoke as provider-rate-limited, provider-blocked, or waiting for full MarketData PHPUnit is superseded for the current source ZIP.
- Future Yahoo/PublicApi rate limit, timeout, network, parse, empty-response, or missing-date outcomes remain valid reason-coded BLOCKED outcomes, but they are not the current final proof state.



## 2026-05-23 — Source Ready Gap Closure Safety Reconciliation

- Provider smoke negative path is fail-closed: empty, invalid, parse-failed, missing selected trade date, timeout, network, header-context mismatch, and rate-limit outcomes produce `provider_smoke_status=BLOCKED`, not false PASS.
- Correction approve policy is strict: only `REQUESTED` may transition to `APPROVED`; all other statuses return `COMMAND_CORRECTION_STATUS_NOT_APPROVABLE`.
- Coverage gate config flags are runtime-enforced and fail-closed: disabled gate or disabled canonical-bar evidence returns `NOT_EVALUABLE`, not readable.


## Source/master read-only recompute boundary
`market-data:eod-indicators:recompute-current` is the implemented current-bars recompute command for this boundary. It does not perform source acquisition, bar ingest, source/master writes, or `eod_bars` writes. It creates a correction-current publication from existing current readable bars and recomputes publication-bound indicator and eligibility artifacts.

"Without updating sector/corporate-action/trading-status/master data" means no writes to source/master tables and no source import commands. It does not mean publication-bound `eod_indicators` context columns are frozen; a new publication may recalculate those fields from existing source rows. A context-freezing technical-only mode must be explicit and separately proven.

## 2026-06-07 recompute-current safety proof

The current-indicator recompute command passed the final safety closure:

- full MarketData PHPUnit: 640 tests / 9539 assertions;
- 807/807 full-range runtime success;
- no source acquisition, bar ingest, source/master writes, or `eod_bars` writes;
- 757 replacement publications used run evidence and 50 unchanged/preserved-current outcomes used correction evidence; all 807 evidence exports were `ADMITTED_COMPLETE`.
- 807/807 final current evidence/replay MATCH/PASS with zero failures/errors/mismatches;
- unchanged correction candidates preserve the current pointer and use correction evidence rather than producing a false publication-not-found failure.

Latest docs-review validation on 2026-06-08 reran `vendor\bin\phpunit` and passed with OK (641 tests, 9547 assertions). This updates the active validation count; the 2026-06-07 807/807 runtime safety proof remains locked.

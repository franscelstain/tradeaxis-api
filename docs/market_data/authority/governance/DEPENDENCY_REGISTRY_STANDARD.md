# Dependency Registry Standard

Verified dependencies use `MD-DEP-xxxx` and are tracked in `development/implementation/MD_DEPENDENCY_REGISTRY.csv`. A dependency record states owner, evidence, impact, current status, and exact resume trigger. Missing external state never authorizes local semantic substitution.

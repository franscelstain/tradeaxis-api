# Legacy Role Extract — C17 — RESEARCH

> **Document Type:** RESEARCH
> **Authoritative Role:** `RESEARCH`
> **Status:** HISTORICAL_EXTRACT / IMMUTABLE
> **Legacy Extract ID:** `LX-WS-0331-RES-02`
> **Legacy Source ID:** `LS-WS-0331`
> **Legacy Work Key:** `C17`
> **Original Path:** `docs/watchlist/audit/WS_C17_OPERATOR_VALIDATION_COMMANDS.md`
> **Original SHA1:** `42CD63983444A66BE40084D8FC4D3FEC6E098F23`
> **Source Sections:** L156-L216 5. IS calibration C17 run 1; L217-L234 6. IS calibration C17 run 2 deterministic rerun
> **Extract Body SHA1:** `D21D121360F11AF1C2490C9B625E60860B8F01C7`
> **Current Authority:** NO

The body below is an exact copy of the registered source sections for this single semantic role. Cross-role authority is intentionally excluded.

---

## 5. IS calibration C17 run 1

Manual command:

```powershell
php artisan watchlist:backtest-is-calibrate --catalog-code=WS_BT_GRID_DOWNSIDE_STABILITY_C17_2026_06 --from=2023-01-02 --to=2025-05-21 --output=storage/app/watchlist/backtest/c17-is-run-1.json --overwrite
```

Expected output markers:

```text
catalog_code=WS_BT_GRID_DOWNSIDE_STABILITY_C17_2026_06
catalog_version=C17
catalog_count=12
catalog_hash=d411bfbee6fb14c17d821aa92e7e0fea06925d67
is_from=2023-01-02
is_to=2025-05-21
strict_is_boundary_all_evaluations=1
oos_service_invoked=0
oos_repository_invoked=0
oos_table_unchanged=1
oos_executed=0
production_ready=0
```

Possible valid statuses:

```text
status=PASS
```

or

```text
status=C17_GRID_FAILED_IS_QUALITY
reason_code=WS_BT_C17_NO_VALID_IS_CANDIDATE
```

Pass criteria for a valid IS candidate:

- `status=PASS`;
- `is_valid_param_count > 0`;
- `best_is_binding` exists;
- all OOS markers remain `0` / unchanged;
- production still `0`.

Fail-as-strategy-quality criteria:

- `status=C17_GRID_FAILED_IS_QUALITY`;
- `reason_code=WS_BT_C17_NO_VALID_IS_CANDIDATE`;
- `is_valid_param_count=0`;
- OOS still not run.

Blocked criteria:

- `status=BLOCKED`;
- canonical gates not reached;
- catalog mismatch;
- OOS mutation;
- non-zero exit code.

## 6. IS calibration C17 run 2 deterministic rerun

Manual command:

```powershell
php artisan watchlist:backtest-is-calibrate --catalog-code=WS_BT_GRID_DOWNSIDE_STABILITY_C17_2026_06 --from=2023-01-02 --to=2025-05-21 --output=storage/app/watchlist/backtest/c17-is-run-2.json --overwrite
```

Expected output markers are the same as run 1.

Pass criteria:

- run 2 status and reason code match run 1;
- run 2 `artifact_hash` equals run 1 `artifact_hash`;
- run 2 `is_trading_date_hash` equals run 1;
- all OOS markers remain unchanged;
- `production_ready=0`.

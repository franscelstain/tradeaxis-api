# Legacy Role Extract — C16 — RESEARCH

> **Document Type:** RESEARCH
> **Authoritative Role:** `RESEARCH`
> **Status:** HISTORICAL_EXTRACT / IMMUTABLE
> **Legacy Extract ID:** `LX-WS-0309-RES-02`
> **Legacy Source ID:** `LS-WS-0309`
> **Legacy Work Key:** `C16`
> **Original Path:** `docs/watchlist/audit/WS_C16_OPERATOR_VALIDATION_COMMANDS.md`
> **Original SHA1:** `B4B21D4B1C22E391D3D1E272C75EFF8D38A9AED5`
> **Source Sections:** L401-L477 8. C16 IS calibration run 1 and run 2
> **Extract Body SHA1:** `F676A343EA4D2409100C676739C0DB13105A1A3D`
> **Current Authority:** NO

The body below is an exact copy of the registered source sections for this single semantic role. Cross-role authority is intentionally excluded.

---

## 8. C16 IS calibration run 1 and run 2

Manual commands:

```powershell
php artisan watchlist:backtest-is-calibrate --catalog-code=WS_BT_GRID_DOWNSIDE_STABILITY_C16_2026_06 --from=2023-01-02 --to=2025-05-21 --output=storage/app/watchlist/backtest/c16-is-run-1.json --overwrite

php artisan watchlist:backtest-is-calibrate --catalog-code=WS_BT_GRID_DOWNSIDE_STABILITY_C16_2026_06 --from=2023-01-02 --to=2025-05-21 --output=storage/app/watchlist/backtest/c16-is-run-2.json --overwrite
```

Expected invariant markers:

```text
catalog_version=C16
catalog_count=12
is_from=2023-01-02
is_to=2025-05-21
strict_is_boundary_all_evaluations=1
oos_service_invoked=0
oos_repository_invoked=0
oos_table_unchanged=1
oos_executed=0
production_ready=0
run1.artifact_hash == run2.artifact_hash
```

If there is no valid IS candidate, expected domain result:

```text
status=C16_GRID_FAILED_IS_QUALITY
reason_code=WS_BT_C16_NO_VALID_IS_CANDIDATE
is_valid_param_count=0
OOS_NOT_ELIGIBLE
OOS_NOT_RUN
production_ready=0
```

If there is a valid IS candidate, expected domain result:

```text
is_valid_param_count > 0
param_id_best_is=<non-empty>
best_is_binding_hash=<non-empty>
artifact deterministic
OOS_NOT_RUN
production_ready=0
```

Pass criteria:

- both calibration runs complete with coherent C16 status;
- artifact hash is deterministic across run 1 and run 2;
- strict IS boundary remains true;
- no OOS invocation occurs;
- production readiness remains `0`.

Fail criteria:

- non-deterministic artifact hash;
- missing C16 identity fields;
- OOS invocation;
- production readiness changes to `1`;
- failed row is promoted as binding;
- C15 changes.

Expected exit code:

```text
0 on completed command with coherent status
non-zero can only be considered domain-valid if status/reason_code/artifact/boundary markers are complete and coherent
```

Claim rule:

Do not claim IS PASS, no-valid-candidate result, artifact hash, or OOS eligibility until operator output is provided.

# Legacy Role Extract — C171 — EVIDENCE

> **Document Type:** EVIDENCE
> **Authoritative Role:** `EVIDENCE`
> **Status:** HISTORICAL_EXTRACT / IMMUTABLE
> **Legacy Extract ID:** `LX-WS-0316-EVD-01`
> **Legacy Source ID:** `LS-WS-0316`
> **Legacy Work Key:** `C171`
> **Original Path:** `docs/watchlist/audit/WS_C171_COMPARATIVE_OFFICIAL_IS_FAILURE_DIAGNOSTIC_OPERATOR_COMMANDS.md`
> **Original SHA1:** `3BACCC3A82DD3D7A201D423104961D0B1226C863`
> **Source Sections:** L3-L10 1. Confirm database; L11-L39 2. Focused tests; L91-L101 4. Hash all outputs
> **Extract Body SHA1:** `607FCD231ABD43A1DE7CCD191DBBF8F9FB9073C1`
> **Current Authority:** NO

The body below is an exact copy of the registered source sections for this single semantic role. Cross-role authority is intentionally excluded.

---

## 1. Confirm database

```powershell
php artisan --version
```

The command must use the main `tradeaxis` database, not `--env=testing`.

## 2. Focused tests

```powershell
vendor\bin\phpunit tests\Unit\Watchlist `
  --filter "WeeklySwingC171ComparativeOfficialIsFailureDiagnosticTest"
```

```powershell
vendor\bin\phpunit tests\Unit\Watchlist `
  --filter "WatchlistBacktestC171StaticGuardTest"
```

```powershell
vendor\bin\phpunit tests\Unit\Watchlist --filter "C171"
```

Then full regression:

```powershell
vendor\bin\phpunit tests\Unit\Watchlist
```

Pass criteria:

- all focused tests pass;
- all C171 tests pass;
- full Watchlist suite passes;
- no migration is required for this stage.

## 4. Hash all outputs

```powershell
Get-ChildItem storage/app/watchlist/backtest `
  -Filter "c171-comparative-official-is-failure-diagnostic*" |
  Sort-Object Name |
  Get-FileHash -Algorithm SHA1 |
  Select-Object Hash, Path
```

Upload the main JSON, hypothesis-lock JSON, and all generated CSV files before implementing the next semantic catalog.

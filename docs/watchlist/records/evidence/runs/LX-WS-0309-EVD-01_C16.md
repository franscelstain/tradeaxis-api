# Legacy Role Extract — C16 — EVIDENCE

> **Document Type:** EVIDENCE
> **Authoritative Role:** `EVIDENCE`
> **Status:** HISTORICAL_EXTRACT / IMMUTABLE
> **Legacy Extract ID:** `LX-WS-0309-EVD-01`
> **Legacy Source ID:** `LS-WS-0309`
> **Legacy Work Key:** `C16`
> **Original Path:** `docs/watchlist/audit/WS_C16_OPERATOR_VALIDATION_COMMANDS.md`
> **Original SHA1:** `B4B21D4B1C22E391D3D1E272C75EFF8D38A9AED5`
> **Source Sections:** L3-L8 Scope; L9-L76 Final operator evidence received; L77-L98 Operator evidence received after initial C16 patch; L99-L139 1. PHPUnit C16 catalog/static tests; L140-L175 2. Candidate universe runtime payload test; L176-L211 3. Scoring runtime payload test; L212-L251 4. Plan grouping runtime guard test; L252-L288 5. Full Watchlist unit suite; L289-L351 6. C16 seed command; L352-L400 7. C16 diagnose batch; L478-L499 Follow-up validation after C15 static guard regex fix; L500-L552 Follow-up after seed approval-list patch
> **Extract Body SHA1:** `A1AF0C9CB9D793C6019EB6EA8EB74C2D0528B72F`
> **Current Authority:** NO

The body below is an exact copy of the registered source sections for this single semantic role. Cross-role authority is intentionally excluded.

---

## Scope

These commands validate the C16 catalog and runtime path.

Do not claim PASS until actual operator output is available.

## Final operator evidence received

C16 operator validation is complete for source/runtime/IS-only calibration.

Completed commands and results:

```text
vendor\bin\phpunit tests\Unit\Watchlist --filter "WatchlistBacktestC16"
OK (12 tests, 553 assertions)

vendor\bin\phpunit tests\Unit\Watchlist
OK (355 tests, 8377 assertions)

php artisan watchlist:backtest-c16-param-grid-seed
status=PASS
catalog_code=WS_BT_GRID_DOWNSIDE_STABILITY_C16_2026_06
catalog_version=C16
catalog_count=12
catalog_hash=0ad1289f79d78787cdca275f0b3f3e2ba90bf8f2
existing_count=12
oos_executed=0
production_ready=0

php artisan watchlist:backtest-is-diagnose-batch --catalog-code=WS_BT_GRID_DOWNSIDE_STABILITY_C16_2026_06 --from=2023-01-02 --to=2025-05-21 --output-dir=storage/app/watchlist/backtest/c16-drilldown --summary=storage/app/watchlist/backtest/c16-drilldown-summary.csv --overwrite
status=PASS
reason_code=WS_BT_IS_FAILURE_DRILLDOWN_BATCH_READY
diagnostic_param_count=12
ready_count=12
blocked_count=0
oos_service_invoked=0
oos_repository_invoked=0
oos_table_unchanged=1
oos_executed=0
production_ready=0

php artisan watchlist:backtest-is-calibrate --catalog-code=WS_BT_GRID_DOWNSIDE_STABILITY_C16_2026_06 --from=2023-01-02 --to=2025-05-21 --output=storage/app/watchlist/backtest/c16-is-run-1.json --overwrite
status=C16_GRID_FAILED_IS_QUALITY
reason_code=WS_BT_C16_NO_VALID_IS_CANDIDATE
is_valid_param_count=0
is_failed_param_count=12
artifact_hash=63698d0c809a1f2124d8218273ba4d34d9c78deb
oos_executed=0
production_ready=0

php artisan watchlist:backtest-is-calibrate --catalog-code=WS_BT_GRID_DOWNSIDE_STABILITY_C16_2026_06 --from=2023-01-02 --to=2025-05-21 --output=storage/app/watchlist/backtest/c16-is-run-2.json --overwrite
status=C16_GRID_FAILED_IS_QUALITY
reason_code=WS_BT_C16_NO_VALID_IS_CANDIDATE
is_valid_param_count=0
is_failed_param_count=12
artifact_hash=63698d0c809a1f2124d8218273ba4d34d9c78deb
oos_executed=0
production_ready=0
```

Final C16 command conclusion:

```text
C16_RUNTIME_VALIDATED=true
C16_IS_DETERMINISTIC=true
C16_GRID_FAILED_IS_QUALITY=true
C16_VALID_PARAM_COUNT=0
C16_FAILED_PARAM_COUNT=12
C16_OOS_ALLOWED=false
C16_PRODUCTION_READY=0
```

No further C16 command should be run except read-only inspection or documentation. OOS remains forbidden because no valid IS candidate exists.

## Operator evidence received after initial C16 patch

The following operator PHPUnit evidence has been received and recorded:

```text
WatchlistBacktestC16: OK (11 tests, 547 assertions)
WatchlistCandidateUniverseService: OK (6 tests, 94 assertions)
WatchlistScoringService: OK (10 tests, 134 assertions)
WatchlistPlanGroupingService: OK (12 tests, 87 assertions)
```

The full Watchlist suite initially failed one legacy C15 static guard because it expected the exact pre-C16 array literal `['C07', 'C14', 'C15']`. C16 correctly extended runtime support to include `C16`, so the C15 guard has been patched to assert that `$extendedCatalogVersions` still contains `C15` without requiring the old exact array.

Next required command before Artisan seed:

```powershell
vendor\bin\phpunit tests\Unit\Watchlist
```

Pass criteria: full Watchlist suite returns `OK (... tests, ... assertions)`. If this fails, stop and fix the PHPUnit regression before running seed, diagnose-batch, or IS calibration.

## 1. PHPUnit C16 catalog/static tests

Manual command:

```powershell
vendor\bin\phpunit tests\Unit\Watchlist --filter "WatchlistBacktestC16"
```

Expected output:

```text
OK (... tests, ... assertions)
```

Pass criteria:

- C16 catalog code/version/count/hash are correct;
- C16 row codes and parameter hashes are deterministic and unique;
- C16 factory maps the new runtime extension mode;
- C16 static guards prove seed command registration and no scheduling;
- C15 hash remains unchanged;
- OOS and production readiness remain false.

Fail criteria:

- any failure/error;
- C16 hash differs from `0ad1289f79d78787cdca275f0b3f3e2ba90bf8f2`;
- C15 hash changes;
- OOS service/repository is invoked;
- production readiness becomes true.

Expected exit code:

```text
0
```

Claim rule:

Do not claim PASS until operator output is provided.

## 2. Candidate universe runtime payload test

Manual command:

```powershell
vendor\bin\phpunit tests\Unit\Watchlist --filter "WatchlistCandidateUniverseService"
```

Expected output:

```text
OK (... tests, ... assertions)
```

Pass criteria:

- C16 receives extended runtime metrics;
- ROC5/ROC20, DV20, volume ratio, ATR, trend/event metrics remain available for downstream scoring/grouping;
- non-watchlist execution/portfolio/broker behavior is not introduced.

Fail criteria:

- missing C16 runtime payload fields;
- C16 mode not recognized;
- any unrelated regression.

Expected exit code:

```text
0
```

Claim rule:

Do not claim PASS until operator output is provided.

## 3. Scoring runtime payload test

Manual command:

```powershell
vendor\bin\phpunit tests\Unit\Watchlist --filter "WatchlistScoringService"
```

Expected output:

```text
OK (... tests, ... assertions)
```

Pass criteria:

- C16 scoring preserves runtime metrics needed by PlanGroupingService;
- score components remain available;
- no recommendation/confirm boundary change occurs.

Fail criteria:

- missing C16 score metrics;
- missing score components;
- unexpected scoring contract regression.

Expected exit code:

```text
0
```

Claim rule:

Do not claim PASS until operator output is provided.

## 4. Plan grouping runtime guard test

Manual command:

```powershell
vendor\bin\phpunit tests\Unit\Watchlist --filter "WatchlistPlanGroupingService"
```

Expected output:

```text
OK (... tests, ... assertions)
```

Pass criteria:

- C16 guard consumes `score_total_min` and `score_total_max`;
- C16 guard rejects score overextension above `0.799999`;
- C16 guard rejects score below `0.700000`;
- C16 guard rejects low volume below `1.5`;
- C16 guard rejects ROC5 outside `-0.020..0.000`;
- PLAN/recommendation/confirm boundary remains unchanged.

Fail criteria:

- C16 rows pass without runtime guard consumption;
- high score `0.8..0.9` is allowed by C16 guard;
- volume `1.0..1.5` is freely allowed;
- confirm or recommendation semantics change.

Expected exit code:

```text
0
```

Claim rule:

Do not claim PASS until operator output is provided.

## 5. Full Watchlist unit suite

Manual command:

```powershell
vendor\bin\phpunit tests\Unit\Watchlist
```

Expected output:

```text
OK (... tests, ... assertions)
```

Pass criteria:

- all Watchlist unit tests pass;
- historical catalog guards remain green;
- C01-C15 immutable expectations remain true;
- no R1/R2/C01-C15 behavior is reinterpreted.

Fail criteria:

- any PHPUnit failure/error;
- any historical catalog hash mutation;
- any OOS/production readiness regression.

Expected exit code:

```text
0
```

Claim rule:

Do not claim PASS until operator output is provided.

## 6. C16 seed command

Manual command:

```powershell
php artisan watchlist:backtest-c16-param-grid-seed
```

Expected output:

```text
status=PASS
catalog_code=WS_BT_GRID_DOWNSIDE_STABILITY_C16_2026_06
catalog_version=C16
catalog_count=12
catalog_hash=0ad1289f79d78787cdca275f0b3f3e2ba90bf8f2
inserted_count=<n or 0>
updated_count=0
existing_count=<n or 0>
r1_immutable=1
r2_immutable=1
c01_immutable=1
c02_immutable=1
c03_immutable=1
c04_immutable=1
c05_immutable=1
c06_immutable=1
c07_immutable=1
c14_immutable=1
c15_immutable=1
oos_executed=0
production_ready=0
```

Pass criteria:

- `status=PASS`;
- catalog identity exactly matches C16;
- `updated_count=0`;
- historical immutable markers are `1`;
- OOS remains not executed;
- production readiness remains `0`.

Fail criteria:

- `status=BLOCKED`;
- historical catalog mismatch;
- C15 changed;
- OOS executed;
- `production_ready=1`;
- unexpected update of immutable rows.

Expected exit code:

```text
0 on PASS
non-zero on BLOCKED/failure
```

Claim rule:

Do not claim seed PASS until operator output is provided.

## 7. C16 diagnose batch

Manual command:

```powershell
php artisan watchlist:backtest-is-diagnose-batch --catalog-code=WS_BT_GRID_DOWNSIDE_STABILITY_C16_2026_06 --from=2023-01-02 --to=2025-05-21 --output-dir=storage/app/watchlist/backtest/c16-drilldown --summary=storage/app/watchlist/backtest/c16-drilldown-summary.csv --overwrite
```

Expected output:

```text
catalog_version=C16
catalog_count=12
diagnostic_param_count=12
ready_count=12
blocked_count=0
missing_runtime_evidence_fields=<empty>
oos_service_invoked=0
oos_repository_invoked=0
oos_table_unchanged=1
oos_executed=0
production_ready=0
```

Pass criteria:

- all C16 rows are runtime-ready;
- missing runtime evidence fields are empty;
- no OOS invocation occurs;
- production readiness remains `0`.

Fail criteria:

- `blocked_count > 0`;
- missing runtime evidence fields are not empty;
- OOS service/repository is invoked;
- production readiness changes to `1`.

Expected exit code:

```text
0 on completed PASS
non-zero should be treated as failure unless output clearly provides a supported domain-valid status and all boundary markers remain valid
```

Claim rule:

Do not proceed to calibration until diagnose-batch output is PASS.

## Follow-up validation after C15 static guard regex fix

Run this first after applying the latest patch:

```powershell
vendor\bin\phpunit tests\Unit\Watchlist --filter "WatchlistBacktestC15StaticGuardTest"
vendor\bin\phpunit tests\Unit\Watchlist
```

Expected marker:

```text
OK (... tests, ... assertions)
```

Fail criteria:
- Any failure in `WatchlistBacktestC15StaticGuardTest`.
- Any failure indicating C15/C16 runtime extension drift.

Claim rule: do not claim full Watchlist PASS until the operator output is provided.

## Follow-up after seed approval-list patch

Operator already proved:

```powershell
vendor\bin\phpunit tests\Unit\Watchlist --filter "WatchlistBacktestC15StaticGuardTest"
vendor\bin\phpunit tests\Unit\Watchlist
```

Observed result before this patch:

```text
OK (5 tests, 46 assertions)
OK (354 tests, 8371 assertions)
php artisan watchlist:backtest-c16-param-grid-seed = BLOCKED / WS_BT_R2_CATALOG_INVALID / catalog_code is not an approved immutable catalog
```

Patch expectation: rerun C16 static/full tests and seed.

Manual command:

```powershell
vendor\bin\phpunit tests\Unit\Watchlist --filter "WatchlistBacktestC16"
vendor\bin\phpunit tests\Unit\Watchlist
php artisan watchlist:backtest-c16-param-grid-seed
```

Expected seed output includes:

```text
status=PASS
catalog_code=WS_BT_GRID_DOWNSIDE_STABILITY_C16_2026_06
catalog_version=C16
catalog_count=12
catalog_hash=0ad1289f79d78787cdca275f0b3f3e2ba90bf8f2
updated_count=0
c15_immutable=1
oos_executed=0
production_ready=0
```

Fail criteria:

```text
status=BLOCKED
WS_BT_R2_CATALOG_INVALID
C15 changed
OOS executed
production_ready=1
updated_count > 0
```

Claim rule: do not claim seed PASS until operator output is provided.
